# Project Context for Claude Code

This file is read automatically by Claude Code when it starts in this folder. It exists so a fresh session has the full picture without the user re-explaining everything.

## What this is

A dissertation project: "A Centralized Disaster Aid Request, Tracking and Distribution Management System for Sri Lanka" — BSc Software Engineering, NSBM Green University. Connects disaster victims, donors, volunteers, and admins on one platform: victims submit aid requests (multiple resource categories per request, each capped), a rule-based algorithm prioritizes them, donors register donations (choosing self-delivery or volunteer-assisted), admins match donations to requests and assign volunteers (volunteers can accept/reject), volunteers deliver, victims confirm receipt **by scanning a QR code the deliverer shows them** (not a same-device button tap). A public map (embedded on the landing page and at `/severity-map`) shows aggregated affected-area severity by district plus live river-gauge levels; a separate admin-only map at `/admin/map` adds exact pinpoint request/donation locations. The UI is available in English, Sinhala, and Tamil. Still to come: photo evidence (deferred, needs Firebase Storage — see "Deferred" below), Google Maps turn-by-turn navigation for volunteers (blocked on the user setting up a billing-enabled Google Cloud project).

The user is a beginner developer working under a tight deadline. Prioritize working code over teaching concepts unless they ask to learn something. Keep explanations short and practical. Give complete, runnable files rather than partial diffs/snippets — the user copy-pastes whole files rather than reviewing line-by-line changes.

## Tech stack (decided, do not suggest alternatives without a strong reason)

- **Backend:** Node.js + Express
- **Database + Auth:** Firebase Firestore + Firebase Authentication (Email/Password provider)
- **Web frontend:** React + Vite + TypeScript + Tailwind CSS, plain `fetch`-based API calls (no React Query/SWR)
- **Mobile:** Expo / React Native, TypeScript. Started — see "Mobile app (Expo / React Native)" below for what's built, what's stubbed, and the setup gotchas.
- **Maps — two different tools for two different jobs, don't conflate them:**
  - **Leaflet + react-leaflet + OpenStreetMap** (free, no API key) — built and live for the admin situation map (`/admin/map`) and the public severity map (`/severity-map`). `react-leaflet@^4` specifically (v5 requires React 19; this project is on React 18.3).
  - **Google Maps Platform (Directions API + Maps JavaScript API / SDK)** — approved by the user, willing to pay — for the volunteer's turn-by-turn navigation screen ("like PickMe"). NOT STARTED — blocked on the user creating a billing-enabled Google Cloud project and API key; **set a budget alert in Google Cloud Console when that happens** so a bug (e.g. a route request fired in a loop) can't produce a surprise bill. Do not use Google Maps for anything Leaflet already handles for free.
- **Weather:** OpenWeatherMap — built (see "Weather (OpenWeatherMap)" below). Needs `OPENWEATHER_API_KEY` in `server/.env` before it returns real data — free tier, no credit card needed, not yet provided.
- **External disaster data — built, using what was actually found by researching the real DMC site (not guessed):**
  - **DMC Emergency Warnings RSS** (`https://www.dmc.gov.lk/index.php?option=com_content&view=category&layout=blog&id=16&Itemid=237&format=feed&type=rss&lang=en`) — real, working, standard RSS 2.0. Parsed with `fast-xml-parser`. Frequently empty (no active warning) — that's DMC's actual state, not a bug.
  - **DMC Situation Reports / Water Level bulletins** — turned out to be **PDF-only**, no structured data on the page itself (title/date/time/download-link rows, actual numbers locked inside each PDF). Deliberately NOT scraped/parsed — PDF text extraction is high-effort and fragile for the value it adds here. Not built.
  - **Live river gauge levels** — via `https://slwaterlevel.mevinu.online/api/data`, an **unofficial community proxy** of the Department of Irrigation's live gauge network (the official `irrigation.gov.lk` real-time page returns 403 for automated requests, so it isn't usable server-side). Returns clean JSON per station: name, basin, water level, alert/minor-flood/major-flood thresholds, coordinates. This is genuinely live and the most useful "affected area" signal available — used for the "River Levels" map layer. **Always labeled as unofficial/indicative in the UI** — it's not a DMC or government-published feed, and could go down or change without notice since it's a hobby project, not an SLA'd service.
  - **GDACS (Global Disaster Alert and Coordination System, EC Joint Research Centre)** — tropical cyclone tracking + general disaster events (earthquakes, floods, droughts, wildfires) worldwide, filtered to ones relevant to Sri Lanka. The GeoJSON REST API originally requested (`gdacsapi/api/Events/geteventlist/SEARCH`) was tested repeatedly (~10 attempts, varied query params, several minutes apart) and consistently failed — either a connection timeout or GDACS's own backend returning `"Timeout expired... obtaining a connection from the pool... max pool size was reached"`, a server-side error on their end. Their RSS feed (`https://www.gdacs.org/xml/rss.xml`) was reliably reachable and has richer per-event data than the REST API's shape was ever confirmed to have — used instead. Same pattern as the DMC PDF-vs-RSS decision below: verify what actually works, use that, document the substitution rather than silently forcing the originally-requested source.
  - This whole area was excluded in the dissertation's own Chapter 1 and the user chose to build it anyway with that tradeoff explicitly acknowledged (see "Known scope deviation" below).
- **Notifications:** Firebase Cloud Messaging — built (see "Push notifications (FCM)" below), replacing the dissertation's SMS requirement (Twilio needs a paid/verified business account; re-confirmed with the user when they asked for "SMS alerts" again, and they chose to stick with FCM rather than take on that cost). Needs `VITE_FIREBASE_VAPID_KEY` in `web/.env` before it can actually deliver a push — not yet provided.
- **Hosting (not done yet):** web → Firebase Hosting; API → Render or Railway free tier

Note: the UI was originally scaffolded in Lovable.ai (defaults to Supabase) — rejected in favor of Firebase, web app rebuilt from scratch. If Supabase references appear anywhere, they're cruft to remove, not a signal to reintroduce it.

## Project structure

```
disaster-aid-system/
  server/
    src/
      config/firebase.js        Firebase Admin SDK init — reads FIREBASE_SERVICE_ACCOUNT_PATH from .env
      middleware/authMiddleware.js   requireAuth, requireRole(...roles)
      routes/
        users.js                 profile creation/lookup (nic/homeAddress required + nic uniqueness for victims), GET /volunteers (admin), PATCH /availability (volunteer, see "Volunteer availability toggle" below)
        requests.js               multi-item request creation (category caps, possibleDuplicate flag via nic OR homeAddress), verify, list
        donations.js              donation CRUD (deliveryMethod self/volunteer) + per-item matching (Haversine distance tiebreak)
        deliveries.js             volunteer assign/accept/reject (with reason), donor self-deliver, status updates, by-request/by-donation lookup, QR-token-gated victim confirmation
        requests.js also has      GET/POST /:id/notes (admin, internal case notes) — see "Internal case notes" below
        stats.js                  GET /api/stats (transparency dashboard) + GET /api/stats/by-area (district-aggregated severity for the public map)
        external.js               GET /alerts (DMC RSS, parsed with fast-xml-parser), GET /water-levels (unofficial river gauge proxy, normalized + status computed), GET /gdacs (GDACS RSS, filtered to Sri Lanka relevance, each event's CAP-XML polygon fetched and attached — see "GDACS disaster alerts" below), GET /district-boundaries (HDX COD-AB admin-2 polygons, downloaded+simplified server-side, see "Real district boundary polygons" below), GET /earthquakes (USGS, Indian Ocean/Bay of Bengal bounding box, tsunami-risk framing — see "USGS earthquakes" below), GET /weather (OpenWeatherMap current conditions for 8 cities, see "Weather (OpenWeatherMap)" below). All public, all cached in-memory (utils/cache.js) to avoid hammering the external source on every page load.
        categories.js              GET /api/categories (any authed user), POST/PATCH /api/categories (admin) — manage the categoryLimits collection, see "Category management" below
        broadcasts.js              GET /active (public), GET / (admin history), POST / (admin, posts a banner + deactivates any current one), PATCH /:id/deactivate (admin) — see "Emergency broadcast banner" below
        auditLog.js                GET / (admin) — last 100 logged actions, see "Audit log" below
        activeDistricts.js         GET / (public), POST / (admin, activate a district), DELETE /:district (admin, deactivate) — see "Active emergency districts" below
      utils/
        priorityScore.js          Rule-Based Priority Allocation Mechanism
        categories.js             getCategoryLimits() — reads the `categoryLimits` Firestore collection (self-seeds from DEFAULT_CATEGORY_LIMITS on first read if empty); no longer a static config object, see "Category management" below
        requestItemStatus.js      computeOverallStatus(items) — derives a request's overall status from its item statuses
        geo.js                    distanceKm(a,b) — Haversine, shared by donation matching and district lookup
        districts.js              Sri Lanka's 25 district centroids + nearestDistrict(location) — approximate (nearest-centroid, not real polygon boundaries); mirrored client-side in web/src/lib/districts.ts for the AdminRequests district filter (see "Filters and search" below)
        cache.js                  getCached(key, ttlMs, fetcher) — tiny in-memory TTL cache used by external.js
        auditLog.js               logAction(actor, action, target, details) — writes to the auditLog collection, see "Audit log" below
      app.js / server.js
  web/
    src/
      i18n.ts                    i18next init (EN/SI/TA, localStorage-persisted) — imported once in main.tsx
      locales/en.json, si.json, ta.json   translation strings. Sinhala/Tamil are AI-translated best-effort, NOT native-reviewed — see i18n.ts comment. Fine for a demo, get checked before real deployment.
      lib/firebase.ts (now also exports `db`, the Firestore client SDK instance, for real-time onSnapshot listeners — see "Live updates" below), auth.ts, api.ts, leafletIcons.ts (fixes Leaflet's default marker icon path under Vite — import once per map page), districts.ts (client-side mirror of server/src/utils/districts.js, see "Filters and search" below), export.ts (exportToCsv/exportToPdf, see "CSV/PDF export" below)
      context/AuthContext.tsx
      components/
        DashboardLayout.tsx, ProtectedRoute.tsx, StatusBadge.tsx (translated status labels)
        AreaSeverityMap.tsx        shared district/gauge map — real district polygons (falls back to centroid circles), see "Maps — built" and "Real district boundary polygons"
        StatCard.tsx, ScrollReveal.tsx, Footer.tsx, LanguageSwitcher.tsx   shared landing/dashboard-page building blocks
        DeliveryQrCode.tsx         renders the confirmation QR (qrcode.react) — shown to whoever has the goods
        QrScanModal.tsx            camera scan modal (html5-qrcode) — shown to the victim to confirm receipt
        EmergencyBanner.tsx        two independent, stackable, dismissible bars: the manual broadcast (live `onSnapshot` on `broadcasts`) and an active-districts bar (live `onSnapshot` on `activeDistricts`, e.g. "Active emergency declared in: Colombo, Kalutara") — dismiss is session-only, doesn't deactivate for others — embedded on Landing.tsx and DashboardLayout.tsx, see "Emergency broadcast banner" and "Active emergency districts" below
        CaseNotesModal.tsx         per-request comment thread (list + add form) opened from AdminRequests.tsx, see "Internal case notes" below
      pages/
        Landing.tsx               sticky header, gradient hero + live stats bar, How-It-Works, embeds AreaSeverityMap (popups off), role cards, Footer
        Login.tsx / Register.tsx  NIC + home address fields (victim role only), LanguageSwitcher
        VictimRequestForm.tsx     multi-category checkbox+quantity picker, caps shown inline, medicine has no cap shown; soft (non-blocking) note if the captured location's district has no active emergency, see "Active emergency districts" below
        VictimMyRequests.tsx      per-item delivery status; "Scan to confirm" opens QrScanModal instead of a plain button
        DonorDonationForm.tsx     deliveryMethod choice (self vs volunteer)
        DonorMyDonations.tsx      self-delivery donors get "Mark as delivered", then DeliveryQrCode once delivered
        AdminRequests.tsx         priority-sorted table, approve/reject, possibleDuplicate + medicine-review + stale-request badges, "Notes" button per row opens CaseNotesModal, checkbox-select pending rows for bulk approve/reject, filter bar (name/disaster type/status/district/date range/stale-only)
        AdminDonations.tsx        find-match, assign-volunteer dropdown (volunteer-delivery only), shows last rejection reason on reassignment
        AdminCategories.tsx       `/admin/categories` — table of existing categories with an inline editable cap + Save per row, plus an "Add a new category" form. See "Category management" below.
        AdminBroadcast.tsx        `/admin/broadcast` — current active banner + Clear button, a form to post a new one (message + severity), and recent history. See "Emergency broadcast banner" below.
        ResourceGapView.tsx       `/admin/resource-gap` — per-category pending-requests vs. available-donations table, sorted by worst shortage first. See "Resource gap view" below.
        AuditLog.tsx              `/admin/audit-log` — who approved/rejected/matched/assigned what and when, last 100 actions. See "Audit log" below.
        VolunteerWorkload.tsx     `/admin/volunteer-workload` — active/completed/rejected delivery counts per volunteer, busiest first, idle volunteers included. See "Volunteer workload view" below.
        AdminActiveDistricts.tsx  `/admin/active-emergencies` — all 25 districts, each independently toggleable active/inactive (not a global switch). See "Active emergency districts" below.
        VolunteerDeliveries.tsx   Accept/Reject (with optional reason prompt), picked_up/delivered, then DeliveryQrCode once delivered, availability toggle in the header (see "Volunteer availability toggle" below). Turn-by-turn navigation NOT built — blocked on Google Maps billing setup.
        SituationMap.tsx          admin-only, `/admin/map`, 4-way toggle (Requests / Areas Affected / River Gauges / Global Alerts (GDACS)) — the GDACS tab draws cyclone hazard-area polygons and colored markers on the map itself, kept on its own tab rather than layered over the other three (see "GDACS disaster alerts" below). Recent DMC warnings list below the map, each with a district picker + "Mark active" quick-action (see "Active emergency districts" below).
        PublicSeverityMap.tsx     public, `/severity-map`, full-size AreaSeverityMap + DMC warnings list + GDACS global disaster alerts list
        TransparencyDashboard.tsx  uses shared StatCard
        Dashboard.tsx             `/dashboard`, shared across all roles — quick links per role, plus a live admin-only KPI row (`AdminKpiRow`, defined in the same file). See "Admin dashboard KPIs" below.
      App.tsx, main.tsx (imports ./i18n and leaflet/dist/leaflet.css globally)
  firestore-rules/firestore.rules   deployed and enforced (see "Live updates" below — this was wrongly believed to be undeployed/test-mode until that assumption was checked and disproven while building live listeners)
  SETUP.md
  CLAUDE.md   this file
```

## Data model (Firestore collections)

### users
`{uid, email, name, role: "victim"|"donor"|"volunteer"|"admin", phone, location, nic, homeAddress, available, createdAt}`. `nic`/`homeAddress` only populated for role "victim" (both `null` otherwise, and `null` for victim accounts created before this field existed) — `nic` is unique across victim accounts, both feed the `possibleDuplicate` check on new requests. Sensitive PII — see Privacy section, non-negotiable constraints on where these can ever appear. `available` (boolean) only meaningful for role "volunteer" — absent on existing volunteer docs pre-dating this field, always treated as `available !== false` (i.e. missing = available) rather than requiring a backfill, see "Volunteer availability toggle" below.

### aidRequests
`{victimId, victimName, victimNic, victimHomeAddress (both normalized, or null for pre-existing accounts), possibleDuplicate, disasterType, items: [{category, quantity, unit, status: "pending"|"matched"|"delivered", donationId}], requestedCategories: string[] (denormalized item categories, for array-contains matching queries), severity, peopleAffected, vulnerableGroups[], location, notes, status: "pending"|"verified"|"rejected"|"in_progress"|"delivered", priorityScore, priorityBreakdown, createdAt, updatedAt, verifiedAt}`. A request can ask for several categories at once, each capped (see `server/src/utils/categories.js` — medicine has no cap but is instead flagged for review). Overall `status` is derived from item statuses via `computeOverallStatus()` — `in_progress` means at least one item matched/delivered but not all delivered yet. `possibleDuplicate` is a flag for admin attention, not a block — set when an existing active request shares the same NIC or home address. `verifiedAt` is set once, the first time a request is approved (never overwritten on a later re-verify) — absent on rejected or still-pending requests; powers the admin dashboard's average time-to-verification KPI, see "Admin dashboard KPIs" below.

### donations
`{donorId, donorName, category, quantity, location, deliveryMethod: "self"|"volunteer", notes, status: "available"|"matched"|"delivered", matchedRequestId, assignedDeliveryId, deliveryStatus (mirrors the active delivery's status, or null), lastRejectionReason (set when a volunteer rejects, cleared when the next one accepts), createdAt, updatedAt}`. One donation still targets exactly one category — a multi-category request gets fulfilled by several separate donations/deliveries, one per category.

### deliveries
`{requestId, donationId, category, volunteerId (null for self-delivery), method: "self"|"volunteer", status: "pending_acceptance"|"accepted"|"rejected"|"picked_up"|"delivered"|"confirmed", rejectionReason, confirmToken (set once status reaches "delivered"), currentLocation: {lat,lng}|null, createdAt, updatedAt}`. Volunteer deliveries start at `pending_acceptance` and need an explicit accept before progressing; a reject resets `donations.assignedDeliveryId`/`deliveryStatus` to null so admin can assign someone else (the request item stays `matched` throughout). Self-delivery donations skip straight to `accepted` (auto-created at match time) and the donor marks `delivered` directly (no `picked_up` step).

### categoryLimits
`{label, max: number|null, unit, createdAt, updatedAt}`, one doc per category, doc ID = a slugified key derived from the label (lowercase, non-alphanumeric → `_`, trimmed). `max: null` means no fixed cap (medicine — always flagged for admin review instead, see `requests.js`). Self-seeds from `DEFAULT_CATEGORY_LIMITS` in `server/src/utils/categories.js` the first time the collection is read empty; after that Firestore is the sole source of truth — the defaults constant is a seed value only, not consulted again.

### broadcasts
`{message, severity: "info"|"warning"|"critical", active: boolean, createdBy, createdByName, createdAt, updatedAt, deactivatedAt}`. Only one broadcast is ever `active: true` at a time — posting a new one deactivates the current one in the same batch write, so there's a full history rather than a single mutable doc. Nothing self-seeds here; an empty collection just means no banner has ever been posted.

### caseNotes
`{requestId, authorId, authorName, text, createdAt}`. Top-level collection (not a subcollection of `aidRequests`, consistent with how `deliveries`/`donations` reference their parent by id rather than nesting) — one doc per note, append-only, no edit or delete. Fetched with a single `where("requestId", "==", ...)` and sorted by `createdAt` in JS rather than `orderBy` in the query, deliberately, to avoid needing a Firestore composite index (see "Internal case notes" below for why this matters).

### auditLog
`{action, actorId, actorName, targetType, targetId, details, createdAt}`. Append-only, admin-read-only. `action` is one of `request.approve` / `request.reject` / `request.bulk_approve` / `request.bulk_reject` / `donation.match` / `delivery.assign` / `district.activate` / `district.deactivate`; `details` is a small action-specific object (e.g. `{victimName}` for a verify, `{ids, count}` for a bulk verify, `{category, matchedRequestId, deliveryId}` for a match, `{requestId, donationId, volunteerId}` for an assign, `{district, sourceAlertTitle}` for a district activate/deactivate). See "Audit log" and "Active emergency districts" below.

### activeDistricts
`{district, activatedAt, activatedBy, activatedByName, sourceAlertTitle}`, doc ID = slugified district name (e.g. `colombo`), one doc per **currently active** district — deactivating deletes the doc rather than flipping a boolean, so "does this doc exist" is the whole active/inactive check. `sourceAlertTitle` is set when activation came from the DMC-alert quick-action on `/admin/map`, `null` for a manual activation from `/admin/active-emergencies`. Public read (no PII), admin-only write. See "Active emergency districts" below.

## Category management

Category caps used to be a hardcoded object requiring a code deploy to change. Now admin-editable at runtime:
- `server/src/utils/categories.js` — `getCategoryLimits()`, the shared async accessor every route uses (self-seeding, see above).
- `server/src/routes/categories.js` — `GET /api/categories` (any authed user — victim/donor forms need it too), `POST /api/categories` (admin, add a new category), `PATCH /api/categories/:key` (admin, adjust an existing category's cap/label/unit).
- `AdminCategories.tsx` (`/admin/categories`) — admin UI: inline cap editing per category + a form to add new ones.
- `VictimRequestForm.tsx` and `DonorDonationForm.tsx` both fetch `GET /api/categories` on mount instead of using a hardcoded list — a category added via the admin UI is immediately requestable and donatable with no code change. `VictimRequestForm.tsx` falls back to the raw Firestore `label` (via i18next's default-value param, `t(\`categories.${key}\`, limit.label)`) for any category not present in the locale JSON files — so new categories show in English until someone adds translated strings, rather than showing a raw i18n key.
- `requests.js` and `donations.js` both validate against `getCategoryLimits()` at request time rather than an imported constant — a cap change or new category takes effect on the very next request, no restart needed.
- Verified live end-to-end: lowered the baby-formula cap (4→2 tins) and confirmed the server rejects a 3-tin request with the updated number in the error message; added a new "Tarpaulins" category and confirmed it appeared in and was usable from both the victim request form and the donor donation form without any code change.

## Emergency broadcast banner

The manual, admin-curated equivalent of the DMC alerts feed — for messages the DMC RSS won't have (e.g. "Cyclone approaching Batticaloa, expect delivery delays").

- `server/src/routes/broadcasts.js` — `GET /active` (public, returns the current banner or `null`), `GET /` (admin, last 20 for history), `POST /` (admin, posts a new banner; body `{message, severity}`, severity defaults to `"warning"`), `PATCH /:id/deactivate` (admin, clears without replacing). Posting a new banner and deactivating the previous one happens in a single Firestore batch, so there's never a moment with two banners active.
- `AdminBroadcast.tsx` (`/admin/broadcast`) — shows the current active banner with a "Clear banner" button, a form to post a new one (message + severity: info/warning/critical), and a short history list below.
- `EmergencyBanner.tsx` — fetches `GET /api/broadcasts/active` once on mount and renders a colored bar (blue/amber/red by severity) with a "Close" button. Closing is **session-only** (the dismissed broadcast's id is stashed in `sessionStorage`) — it hides the banner for that browser tab's session, it does **not** deactivate it for anyone else. Only an admin clearing it from `/admin/broadcast` actually removes it for everyone.
- Embedded in two places: `Landing.tsx` (top of the public page, above the header) and `DashboardLayout.tsx` (top of the page, above the sidebar+content row, so every logged-in role — victim, donor, volunteer, admin — sees it regardless of which dashboard page they're on).
- No live-update mechanism yet — the banner is fetched once per page load, so if an admin posts a banner while a user already has a page open, they won't see it until they navigate or refresh. This is the same gap the upcoming "Live updates via Firestore `onSnapshot`" task (next up after this one) is meant to close — worth switching `EmergencyBanner.tsx` to a real-time listener at the same time rather than handling it twice.
- Verified live end-to-end: posted a "critical" banner as admin, confirmed it rendered on both `/admin/broadcast` and the public landing page (logged out), dismissed it client-side and confirmed the dismissal survived a reload but the banner reappeared for a fresh session, then cleared it from the admin page and confirmed `GET /api/broadcasts/active` returned `null` again everywhere.

## Internal case notes

A small, per-request comment thread for admin-to-admin handoff context ("called the victim, they confirmed access road is passable") — separate from the emergency broadcast banner, which is public-facing and one-at-a-time.

- `server/src/routes/requests.js` — `GET /:id/notes` and `POST /:id/notes` (both admin-only), reading/writing the top-level `caseNotes` collection. Both 404 if the request itself doesn't exist.
- `CaseNotesModal.tsx` — a modal (same overlay pattern as `QrScanModal.tsx`) showing the thread in chronological order plus a textarea + "Add note" button. Opened from a new "Notes" column/button on every row of `AdminRequests.tsx`.
- **Hit a real bug while building this, worth remembering:** the first version of `GET /:id/notes` used `.where("requestId", "==", id).orderBy("createdAt", "asc")`, which is a valid-looking query that actually throws `FAILED_PRECONDITION: The query requires an index` at runtime — Firestore needs a composite index for equality-filter-plus-orderBy-on-a-different-field, and none exists in this project (no `firestore.indexes.json`, everything so far has relied on either single-field queries or sorting in JS). Fixed by dropping the `orderBy` and sorting the fetched array by `createdAt` in JS instead — the same pattern `GET /api/requests` already uses for `priorityScore`. **If a future feature needs a Firestore query that filters on one field and orders by another, default to sorting in JS unless there's a real reason to push it to Firestore** — it avoids a deploy-time index dependency this project has otherwise avoided entirely.
- Notes are append-only by design — no edit or delete endpoint — so the handoff trail stays trustworthy as a lightweight record, not because it's meant to be a full audit log (that's its own upcoming task).
- Verified live: added two notes to a request, confirmed they rendered in order with author + timestamp, then opened a different request's notes and confirmed the thread was empty and correctly scoped (not shared across requests).

## Live updates (Firestore `onSnapshot`)

Directly answers dissertation Research Question 2 ("how can real-time digital information sharing improve collaboration") — demonstrable live in the viva: open `/admin/requests` in one browser, submit a request as a victim in another, watch it appear with no refresh.

- **Architecture: reads go live, writes stay on the Express API.** `AdminRequests.tsx`, `AdminDonations.tsx`, and `EmergencyBanner.tsx` now subscribe directly to Firestore via the client SDK's `onSnapshot` instead of a one-time `apiFetch` — `web/src/lib/firebase.ts` exports `db` (`getFirestore(firebaseApp)`) for this. Every *write* (verify, match, assign, post a banner, etc.) still goes through the Express backend exactly as before, because that's where business-rule validation lives (priority scoring, category caps, one-active-banner-at-a-time). The listener just means the UI updates the instant Firestore's data changes, regardless of whether that change came from this browser tab, another admin's tab, or the backend — no manual `load()`/refetch needed after a mutation.
- `AdminRequests.tsx` subscribes to the whole `aidRequests` collection unfiltered and sorts by `priorityScore` in JS (same sort as before, just now live). `AdminDonations.tsx` subscribes to `donations` the same way; its volunteer list stays a one-time `apiFetch` since that data doesn't need to be live. `EmergencyBanner.tsx` uses a `where("active", "==", true), limit(1)` query instead of `GET /api/broadcasts/active` — this is the one live listener that also runs for **logged-out** users on the public landing page.
- **Important discovery made while building this, corrects a wrong assumption repeated across earlier handoffs of this file:** Firestore security rules were believed to be undeployed/"test mode." They were not — `firestore-rules/firestore.rules` (deployed via `firebase deploy --only firestore:rules`, config in `firebase.json`/`.firebaserc`) was already live and enforced, which is exactly why building this feature surfaced a real bug: `broadcasts`, `categoryLimits`, and `caseNotes` had **zero rule coverage**, so `EmergencyBanner.tsx`'s direct read was silently denied (`permission-denied`) even though the same data was readable fine through the Express backend (Admin SDK bypasses rules entirely — this is why the gap went unnoticed until a client made a *direct* Firestore read). Fixed by adding rule blocks for all three collections and redeploying — confirmed with the user before deploying, since it's a live security change. See the corrected note in "Privacy and security notes" below.
- Verified live end-to-end, three separate ways: (1) approved a request via a `curl` call to the Express API while `/admin/requests` stayed open in a browser with no reload — the row's status and action buttons updated instantly; (2) mutated a donation's `status` field directly via the Admin SDK (bypassing the Express API entirely, simulating any external writer) while `/admin/donations` stayed open — same instant update, confirming this isn't just an artifact of the app's own fetch calls; (3) posted and then deactivated a broadcast via the API while the **logged-out** public landing page stayed open — the banner appeared and disappeared live, proving the public read path works under the newly-deployed rules, not just the authenticated admin path.
- Not made live in this pass (kept in scope with what the user asked for): `AdminCategories.tsx` and `AdminBroadcast.tsx`'s own history view still use one-time fetches. Extend them the same way later if it's ever worth it — lower value since caps/banners change far less often than requests/donations.

## Bulk approve/reject

For a real surge, going row-by-row through pending requests doesn't scale — this lets an admin select several at once.

- `PATCH /api/requests/bulk-verify` (admin) — body `{ids: string[], approve: boolean}`. Looks up all ids, batch-updates whichever ones actually exist, and reports `{status, updated, skipped}` rather than failing the whole call over one bad id.
- `AdminRequests.tsx` — a checkbox appears only on `pending` rows (mirrors the existing per-row Approve/Reject visibility), plus a "select all pending" checkbox in the header. Selecting any row reveals a bulk action bar (`N selected` + Approve/Reject/Clear). A `useEffect` keyed on the live `requests` data prunes the selection automatically if a selected row's status changes from somewhere else (another admin tab, a direct write) — so the selection can never point at a row that's no longer pending.
- Because `AdminRequests.tsx` already runs on the live `onSnapshot` listener (see "Live updates" above), no manual refetch happens after a bulk action — the listener picks up the batched Firestore write and the rows update themselves, same as every other write path on this page.
- Verified live: selected both existing pending requests via "select all," clicked "Approve selected," confirmed both flipped to `Verified` with no reload and the bulk bar/checkboxes disappeared correctly (no longer pending); reverted them back to `pending` afterward to leave demo data as found.

## Filters and search

The requests table stops being usable past ~30 rows without this — a filter bar sits above the table in `AdminRequests.tsx`.

- Filters: free-text search on `victimName` (case-insensitive substring), `disasterType` (fixed dropdown — flood/landslide/cyclone/drought/other, same list `VictimRequestForm.tsx` uses), `status`, `district`, and a `createdAt` date range (`<input type="date">` from/to, compared as ISO-string prefixes rather than parsed `Date` objects — works because `createdAt` is always a full ISO timestamp and lexicographic comparison of an ISO prefix behaves correctly here).
- All filters combine with AND, computed client-side via `useMemo` over the already-live `requests` array from the `onSnapshot` listener — no new backend endpoint, filtering is just a derived view over data that was already being fetched.
- **District isn't a stored field** on `aidRequests` — same as the public severity map, it's derived on the fly from `location` via nearest-centroid matching. Rather than adding a backend endpoint for this, `server/src/utils/districts.js`'s `DISTRICTS` list and `nearestDistrict()` function were mirrored into `web/src/lib/districts.ts` (identical Haversine formula, verified to match `server/src/utils/geo.js`'s exactly) so the filter can compute it client-side with no round trip. The district dropdown's options are derived from whatever districts are actually present in the current data (via the same `nearestDistrict` call), not the full static list of 25 — keeps the dropdown relevant instead of mostly-empty on a small demo dataset.
- Bulk-select ("select all pending") now operates on the *filtered* set, not the full dataset — selecting "all" while a filter is active only selects the visible pending rows, which is the behavior an admin would expect.
- A "Showing X of Y" counter and a "Clear filters" button (only rendered when a filter is active) sit at the end of the filter bar.
- Verified live: name search narrowed correctly to substring matches; status filter isolated exactly the delivered requests; district filter combined with status correctly returned zero results for a status/district pair with no overlap and the expected rows for one that did; a future "from" date correctly zeroed out all 7 seed requests, proving the date-range comparison works; "Clear filters" reset every field and made the button disappear.

## Resource gap view

Directly answers the "resource shortfall alerts" idea already sketched in the user's own Figure 1 rich picture — a panel showing categories in high demand vs. low donation supply (e.g. "12 pending water requests, 2 available water donations").

- `ResourceGapView.tsx` (`/admin/resource-gap`) — subscribes live to both `aidRequests` and `donations` via `onSnapshot` (same pattern as `AdminRequests.tsx`/`AdminDonations.tsx`), plus a one-time `GET /api/categories` fetch for labels/units. No new backend endpoint — this is a derived view computed client-side (`useMemo`) over data that's already being streamed for the other admin pages.
- Per category: **pending requests** = count of request *items* with `status === "pending"` in that category (not summed quantities — matches the "12 pending water requests" phrasing, i.e. how many separate needs are outstanding); **available donations** = count of donations with `status === "available"` in that category. `gap = pending - available`.
- Rows sort by `gap` descending, so the worst shortage is always at the top. Each row gets a badge: red "Short by N" when `gap > 0`, green "Covered" when supply meets or exceeds demand, gray "No activity" when both are zero (keeps a mostly-idle demo dataset from looking alarming).
- Verified live: cross-checked the rendered counts against a direct API tally (medicine: 1 pending/0 available → "Short by 1"; water: 0 pending/1 available → "Covered"; everything else 0/0 → "No activity"), then inserted a food donation directly via the Admin SDK (bypassing the Express API entirely) and confirmed a new "Food" row appeared with the correct count and "Covered" badge with no reload — proving the live-listener architecture extends cleanly to a derived/aggregated view, not just raw record lists.

## Stale request flags

Auto-highlights anything sitting unactioned too long, so an urgent case doesn't silently sink to the bottom of a long, priority-sorted list.

- Purely a derived/computed flag on `AdminRequests.tsx` — no new backend endpoint, no new Firestore field. `isStale(r)` is true when `status` is `"pending"` or `"verified"` (i.e. no item has matched yet — `"in_progress"` already means something moved) **and** `createdAt` is older than an admin-adjustable threshold (number input in the filter bar, default 6 hours).
- Stale rows get an amber `⏱ Stale (Nh)` badge in the existing Flags column (age shown in hours, or days once past 48h) and a subtle amber row tint — distinct from the blue selection tint, which still takes priority when a row is both selected and stale.
- A "Stale only" checkbox in the filter bar composes with every other filter (name/type/status/district/date range) the same way, since it's just one more clause in the same `filteredRequests` `useMemo`.
- **One real wrinkle worth remembering:** staleness changes purely because *time passes*, not because Firestore data changed — the existing `onSnapshot` listener only re-renders on a write. Without something extra, a request sitting at 5h59m wouldn't visibly flip to "stale" until some unrelated write happened to touch the collection. Fixed with a `setInterval` that ticks a dummy state value every 60 seconds purely to force a re-render (included as a dependency of the `filteredRequests` memo) — cheap, and the right fix for any future feature whose truth value depends on wall-clock time rather than data.
- Verified live: backdated one seed request's `createdAt` to 9 hours ago directly via the Admin SDK and confirmed it picked up "⏱ Stale (9h)" and the amber tint with no reload; independently, a different seed request already showed "⏱ Stale (24h)" on its own from real elapsed time, confirming the age math wasn't just matching the one forced test case. Toggling "Stale only" correctly narrowed the table to exactly those two rows ("Showing 2 of 7"); raising the threshold to 30h correctly excluded both ("Showing 0 of 7"). Reverted the backdated timestamp afterward.

## Audit log

Directly ties to the ethics chapter's commitment to reporting findings "fully and accurately" — a real, queryable trail of who approved/rejected/matched/assigned what and when, not just a claim that the system supports accountability.

- `server/src/utils/auditLog.js` — `logAction(actor, action, target, details)`, a small reusable helper. Wired into exactly the actions the user asked to cover ("who approved/rejected/assigned what") rather than every possible admin write, to keep this pass scoped: `PATCH /:id/verify` and `PATCH /bulk-verify` in `requests.js` (`request.approve`/`request.reject`/`request.bulk_approve`/`request.bulk_reject`), `POST /:id/match` in `donations.js` (`donation.match`), and `POST /` in `deliveries.js` (`delivery.assign`, the volunteer-assignment step). Category/broadcast/case-note CRUD is **not** logged yet — easy to extend with the same helper later if that's ever wanted.
- `GET /api/audit-log` (admin) — last 100 entries, `orderBy("createdAt", "desc").limit(100)`. Safe without a composite index here specifically because there's no `where` filter alongside the `orderBy` — the composite-index requirement that bit "Internal case notes" only kicks in when you combine an equality filter with an orderBy on a different field.
- `AuditLog.tsx` (`/admin/audit-log`) — a one-time fetch (like `AdminBroadcast.tsx`'s history view, not a live `onSnapshot` listener — deliberately kept out of the live-updates architecture for now, consistent with the "lower value to make live" reasoning already used for caps/banner history). Renders a color-coded action badge plus a human-readable one-line description per action type (`describeEntry()`).
- Deliberate architectural choice: writes still go through the Express API as the single source of truth; `logAction` is called from inside each route handler right after the Firestore write it's documenting succeeds, not via some separate hook or middleware — keeps the "what got logged" mapping to "what the code actually does" directly visible in each route file rather than inferred from a generic wrapper.
- Verified live end-to-end: triggered all four wired actions directly against the API — a single approve, a bulk-reject of two requests, a donation match, and a volunteer assignment — and confirmed each produced exactly one correctly-shaped `auditLog` entry, in the right order, rendering correctly on `/admin/audit-log` with accurate human-readable details (e.g. "Donation mWtf9XwHo9mHi2QrqES6 (water) → request YCu0nUm2Evcyr6rKq3qA", "Volunteer WRhx... → request YCu0n..."). All test requests/donations/deliveries and the test audit entries themselves were cleaned up afterward.

## CSV/PDF export

Lets an admin hand data upward (supervisor, examiners, a real DMC-adjacent org) without needing direct database access.

- `web/src/lib/export.ts` — two generic helpers, `exportToCsv(filename, headers, rows)` and `exportToPdf(filename, title, headers, rows)`, both pure client-side (a Blob + `<a download>` for CSV, `jspdf` + `jspdf-autotable` for PDF — new dependencies, added specifically for this feature). Both take plain `string[]` headers and `(string|number)[][]` rows, so the same two functions work for any table on the site, not just these two.
- **Requests** (`AdminRequests.tsx`) — "Export CSV"/"Export PDF" buttons sit next to the existing "Showing X of Y" counter and export exactly `filteredRequests` — i.e. whatever the filter bar currently shows, including the date range already built for "Filters and search". This is what gives "for a given period" for free: set a From/To range, then export, no separate period picker needed.
- **Donations** (`AdminDonations.tsx`) — had no filter bar yet (out of scope to build one just for this), so a small standalone "Period from/to" date pair was added purely to scope the export, plus the same two buttons. Both disable when the current period has zero rows.
- Columns: requests export Priority/Victim/Disaster/Items/Severity/Affected/Status/District/Created At (items joined as `category xN; category xN`); donations export Donor/Category/Quantity/Delivery/Status/Created At.
- Verified live: downloaded and read back both formats from both pages. CSV rows matched the on-screen table exactly (multi-item requests correctly semicolon-joined). The PDF included a title, a generated-at timestamp + row count, and a correctly formatted table — confirmed by actually opening the downloaded file, not just checking the click didn't error. Confirmed the donations period filter's disabled state: setting "from" to a future date correctly zeroed the row count and disabled both export buttons.

## Volunteer workload view

Small addition once there are multiple volunteers in test data — a table showing deliveries per volunteer so an admin doesn't overload one person while others sit idle.

- `VolunteerWorkload.tsx` (`/admin/volunteer-workload`) — one-time `GET /api/users/volunteers` fetch (volunteer roster changes rarely) plus a live `onSnapshot` on the `deliveries` collection (same live-listener pattern as everywhere else). No new backend endpoint — purely a client-side aggregation (`useMemo`) over data that's already flowing.
- Per volunteer: **Active** = deliveries in `pending_acceptance`/`accepted`/`picked_up`; **Completed** = `delivered`/`confirmed`; **Rejected** = `rejected`; **Total assigned** = every delivery ever assigned to them regardless of status. Rows sort by Active descending (busiest first), then Total.
- Starts from the full volunteer roster, not just volunteers with deliveries — a volunteer with zero assignments still gets a row with an "Idle" badge, which is the point of the feature (surfacing who's sitting idle, not just who's busy). Active count also gets a badge: gray "Idle" at 0, plain number at 1–2, amber "N — High load" at 3+.
- **One real data-quality find while verifying this, not a bug in the new code:** one pre-existing test delivery had `status: "assigned"`, a value that doesn't appear anywhere in `deliveries.js`'s actual status enum (`pending_acceptance → accepted/rejected → picked_up → delivered → confirmed`) — leftover from an earlier iteration of the schema before this session. It correctly counts toward **Total assigned** but doesn't fall into Active/Completed/Rejected (none of which match "assigned"), so those three columns don't sum to the total for that one row. This is accurate given the data, not a display bug — flagging here in case a future session wonders why the numbers don't add up for volunteer1. Worth a one-off cleanup of that stray doc if it's ever noticed during a demo.
- Verified live: cross-checked rendered counts against a direct Firestore query (Volunteer Reject: 0 completed/2 rejected; Test Volunteer: 2 completed/0 rejected; volunteer1: 0/0 with 1 stray "assigned" delivery in Total only) — exact match. Then inserted three `picked_up` test deliveries for volunteer1 directly via the Admin SDK and watched their row jump to the top with a live "3 — High load" badge and Total updating from 1 to 4, with no reload. Test deliveries cleaned up afterward.

## Volunteer availability toggle

Lets volunteers mark themselves active/inactive so the admin's assignment dropdown only shows people actually available right now, instead of everyone regardless of whether they're currently free.

- `PATCH /api/users/availability` (volunteer-only) — body `{available: boolean}`, updates the caller's own `users` doc. No admin override endpoint — only the volunteer themselves can flip their own flag, consistent with how `nic`/`homeAddress` etc. are self-owned fields.
- `VolunteerDeliveries.tsx` — a pill toggle next to the page title ("Available for new deliveries" in green / "Unavailable" in gray), fetches current status from `GET /api/users/me` on mount and calls the new endpoint on click. Deliberately kept local to this component rather than added to the global `AuthContext` profile type — nothing else in the app needs to react to a volunteer's own availability changing, so there was no reason to widen a shared type for one page.
- `AdminDonations.tsx` — the assign-volunteer `<select>` now filters `volunteers` down to `available !== false` before rendering options (excluded, not just disabled — matches "only shows people actually available"). If every registered volunteer is currently unavailable, the dropdown is replaced with "No volunteers currently available." instead of an empty select; a similar note appears at the bottom of the page distinguishing "no volunteers exist yet" from "volunteers exist but none are available."
- `VolunteerWorkload.tsx` also picked up an "Availability" column (green "Available" / gray "Unavailable" badge) since it's a natural companion view — an admin deciding who to assign wants both "are they free" and "are they already overloaded" in one place.
- **Backwards compatibility:** every check uses `available !== false` rather than `available === true`, so volunteer accounts created before this field existed (no `available` field at all) are correctly treated as available by default — no backfill migration needed.
- Verified live: toggled volunteer1 to unavailable from their own `/deliveries/mine` page, confirmed the change persisted in Firestore directly via the Admin SDK, then confirmed from the admin side that volunteer1 disappeared from `AdminDonations.tsx`'s assignment dropdown (down to 2 of 3 volunteers) and showed "Unavailable" on `/admin/volunteer-workload` — both live, no reload. Reverted afterward.

## Automatic volunteer assignment — planned change, not yet built

**Decided while writing the thesis's Chapter 4 (System Requirement Specification), not yet implemented in code.** Volunteer assignment today is manual: `POST /api/deliveries` requires an admin to pick a specific volunteer from the `AdminDonations.tsx` dropdown (filtered to `available !== false`, see "Volunteer availability toggle" above). The user has decided to change this so the **system auto-assigns** a volunteer to a matched, volunteer-delivery donation instead of requiring an admin to pick one — the volunteer still explicitly accepts or rejects the assignment (existing `pending_acceptance → accepted/rejected` flow, unchanged), so a bad or unwanted auto-match is never forced on anyone.

This restores the original workflow already described in the dissertation's Chapter 02 (§2.5.3, step 6): "Available volunteers are assigned to matched request-donation pairs based on proximity to the donation pickup location" — the manual-dropdown version built so far was actually a deviation from that, not the other way around.

**Still open, needs a decision before implementation:** the exact auto-assignment rule. The obvious candidate is nearest-available-volunteer by Haversine distance to the donation's pickup location (reusing `server/src/utils/geo.js`'s `distanceKm()`, same function already used for donation-to-request matching in `donations.js`), tie-broken by current workload (fewest active deliveries, from the same aggregation `VolunteerWorkload.tsx` already computes) if two volunteers are equidistant. Whether this replaces the manual dropdown entirely, or keeps it as an admin override for edge cases, is also still open — worth confirming with the user before building it, not assumed here.

**Implementation sketch for whichever session picks this up:** likely lives in `POST /api/deliveries` (or a new endpoint triggered automatically right after `POST /:id/match` in `donations.js`, so a volunteer-delivery donation gets a volunteer assigned in the same flow as being matched to a request, rather than requiring a separate admin action at all). Needs `available !== false` filtering preserved (don't assign to an unavailable volunteer), and should still write the same `deliveries` doc shape (`status: "pending_acceptance"`) so nothing downstream (accept/reject, QR confirmation, audit log, volunteer workload view) needs to change.

## Admin dashboard KPIs

The last item on the user's operational-features list — turns `/dashboard` from a quick-links menu into an actual command-center view for the admin role specifically (other roles are unchanged).

- `Dashboard.tsx` — a new `AdminKpiRow` component, rendered only when `profile.role === "admin"`, sits above the existing quick-links grid on the same shared `/dashboard` route. Subscribes live via `onSnapshot` to `aidRequests`, `donations`, and `deliveries` (same pattern as every other admin view) and renders four `StatCard`s (the same shared component used on `Landing.tsx`/`TransparencyDashboard.tsx`):
  - **Pending requests** — count of `aidRequests` with `status === "pending"`.
  - **Unmatched donations** — count of `donations` with `status === "available"`.
  - **Active deliveries** — count of `deliveries` with `status` in `pending_acceptance`/`accepted`/`picked_up` (same `ACTIVE_STATUSES` concept as "Volunteer workload view").
  - **Avg. time to verification** — average of `verifiedAt - createdAt` across every request that has ever been verified (regardless of what status it's since moved to), formatted as minutes/hours/days depending on magnitude. Shows "—" when no request has been verified yet, rather than 0 or NaN.
- **Needed a real backend change to be accurate, not just a frontend view:** averaging verification time requires knowing exactly when each request was first approved. `updatedAt` doesn't work for this — it gets overwritten by every subsequent change (matched, delivered, etc.), so it stops meaning "time of verification" the moment a verified request progresses further. Added a `verifiedAt` field to `aidRequests`, set once in both `PATCH /:id/verify` and `PATCH /bulk-verify` the first time a request is approved (`approve === true && !doc.data().verifiedAt`) — never overwritten on a later re-verify, and never set at all for rejected requests, so the average is only ever computed over genuinely-verified requests.
- Verified live: cross-checked initial KPI values against a direct API tally (2 pending, 1 unmatched donation, 0 active deliveries, "—" for avg. verification since no request had a `verifiedAt` yet) — exact match. Then approved one pending request via the API while `/dashboard` stayed open with no reload — Pending requests dropped 2→1 and Avg. time to verification populated with "2.7 days" instantly, correctly reflecting that request's real elapsed time since submission. Reverted the test request back to pending (and cleared its `verifiedAt`) afterward.

## Active emergency districts

Not from the original "one at a time" list — a follow-on request refining and extending the emergency broadcast banner. Came with five explicit, non-negotiable constraints from the user, all honored by design:

1. **"Never hard-block submission. The form is always open... non-negotiable given your own research problem statement."** `server/src/routes/requests.js`'s `POST /` handler has zero references to `activeDistricts` anywhere — not "checked but always allows," genuinely no code path connects them. District status only ever adds an informational note; grep for `activeDistrict` in that file to re-confirm this if it's ever in doubt.
2. **"Admin marks specific districts as 'active emergency,' not a single system-wide toggle — Sri Lanka's disasters are usually localized."** Modeled as one Firestore doc per *currently active* district (collection `activeDistricts`, doc ID = slugified name) rather than a single boolean — each of the 25 districts is independently activated/deactivated. Deactivating **deletes** the doc rather than flipping a flag to false, so "does this doc exist" is the entire active-check, both server- and client-side.
3. **"DMC alerts feed into this as a suggestion, not an automatic switch... a one-click 'mark [district] as active' button right next to each incoming alert, rather than trying to auto-parse district names out of bulletin text reliably."** `SituationMap.tsx`'s existing DMC alerts panel (`/admin/map`) now has a district `<select>` + "Mark active" button under every alert. The admin reads the bulletin and picks the district themselves — deliberately no NLP/regex attempt to extract a district from `alert.title`/`alert.summary`, exactly per the instruction. The activation call includes `sourceAlertTitle` so `/admin/active-emergencies` and the audit log both show which DMC bulletin (if any) prompted it, purely for context — it plays no role in the activation logic itself.
4. **"If a victim submits from a district that's not currently marked active, show a soft note, not a block."** `VictimRequestForm.tsx` fetches `GET /api/active-districts` once on mount, computes `nearestDistrict(location)` (same client util as `AdminRequests.tsx`'s filters) once the victim captures their location, and shows *"No declared emergency currently active in your area — your request will still be reviewed."* directly under the location button when their district isn't in the active set. Purely additive to existing markup — never touches `error` state, never disables the submit button, never appears in `handleSubmit`'s validation at all.
5. **"This also powers the emergency broadcast banner — same underlying 'active districts' data drives both."** `EmergencyBanner.tsx` now renders **two independent, stackable bars**: the existing manual broadcast (unchanged) and a new one driven by a live `onSnapshot` on `activeDistricts` — e.g. *"Active emergency declared in: Colombo, Kalutara"* — shown whenever the active-district list is non-empty, on both the public landing page and every logged-in dashboard. Both bars are independently dismissible per session (the district bar's dismiss key is the sorted, joined district list, so a *change* to which districts are active — not just any Firestore write — makes it reappear even if the old set was dismissed).

Also built: `AdminActiveDistricts.tsx` (`/admin/active-emergencies`) — the primary management surface, a full table of all 25 districts (from the same `DISTRICTS` list `AdminRequests.tsx`'s filters use) with live status, who activated each one, its source (DMC alert title or "Manual"), and an activate/deactivate button per row.

- `server/src/routes/activeDistricts.js` — `GET /` (public), `POST /` (admin, body `{district, sourceAlertTitle?}`, validates against the real 25-district list, idempotent — reactivating just refreshes the record), `DELETE /:district` (admin). Both writes call `logAction` (`district.activate` / `district.deactivate`), extending `AuditLog.tsx`'s `ACTION_LABELS`/`describeEntry` to cover them.
- Needed a new Firestore rule block (`activeDistricts`: public read, admin write) since `EmergencyBanner.tsx` and `AdminActiveDistricts.tsx` both read it directly via the client SDK, same reasoning as `broadcasts`. Deployed via `firebase deploy --only firestore:rules` — confirmed with the user before deploying, consistent with how the live-updates and category/broadcast/case-note rule additions were handled earlier.
- Verified live end-to-end: activated Kalutara from `/admin/active-emergencies`, watched it appear instantly on the public landing page's banner ("Active emergency declared in: Kalutara") with no reload; confirmed via direct API calls that `POST /api/active-districts` rejects an invalid district name (400) and that `POST /api/requests` has no dependency on district state at all (grep, not just testing, per constraint #1 above); confirmed the audit log recorded both the activate and a subsequent deactivate correctly. Deactivated Kalutara afterward to leave demo data clean.
- **Two things not exercisable in this dev/tooling environment, noted rather than glossed over:** (a) the DMC RSS feed happened to be empty at verification time (its documented normal state — see "External disaster data" above), so the per-alert quick-action button couldn't be clicked against a real bulletin; the same `POST /api/active-districts` call it makes (including `sourceAlertTitle`) was verified directly instead. (b) `navigator.geolocation` isn't available in the browser automation tooling used for verification (same limitation already noted for the QR camera-scan flow), so the victim-form soft note couldn't be click-tested through a real "capture location" round trip — verified instead by confirming `GET /api/active-districts` returns correct data and that `nearestDistrict()` is the same already-verified function used elsewhere. Worth a manual phone/browser check before treating this as fully proven.

## GDACS disaster alerts

Tropical cyclone tracking and general disaster event alerts (earthquakes, floods, droughts, wildfires) from GDACS, filtered to ones relevant to Sri Lanka — requested as a new integration, not from the original feature list.

- **The originally-requested GeoJSON REST API doesn't currently work — verified by repeated testing, not assumed.** `https://www.gdacs.org/gdacsapi/api/Events/geteventlist/SEARCH` was hit ~10 times over several minutes, with and without query params (`eventtypes=TC`, date ranges). Every attempt either timed out at the connection level or returned a 400 with GDACS's own backend error: `"Timeout expired. The timeout period elapsed prior to obtaining a connection from the pool... max pool size was reached"` — a server-side resource-exhaustion error on GDACS's end, not a malformed-request problem on this project's end. Flagged to the user directly (their message had asserted the endpoint was "confirmed working") rather than silently substituting; the user chose to proceed with the working alternative below.
- **Built against GDACS's own RSS feed instead** (`https://www.gdacs.org/xml/rss.xml`) — reliably reachable, and turned out to have richer structured per-event data than the REST API's response shape was ever actually confirmed to have (this project never got a single successful response from it to compare). Parsed with `fast-xml-parser`, same as the DMC alerts feed. Exact same substitution pattern already established for DMC (their RSS works, their PDF-only situation reports don't get scraped) — verify what's actually usable, use that, document why rather than force the nominally-requested source.
- `server/src/routes/external.js`'s new `GET /gdacs` — public, cached 15 min (`utils/cache.js`, same pattern as `/alerts` and `/water-levels`). Each RSS `<item>` carries `gdacs:eventtype` (TC/EQ/FL/DR/WF/...), `gdacs:eventname` (e.g. "DOLPHIN-26" for a named cyclone), `gdacs:alertlevel` (Green/Orange/Red), `gdacs:country`/`gdacs:iso3`, a `geo:Point`, and a `gdacs:bbox` (`lonmin lonmax latmin latmax`) — confirmed by parsing the real feed and inspecting actual events (a live Pacific cyclone, Canada/Japan earthquakes), not guessed from documentation.
- **Sri Lanka relevance filter (`isRelevantToSriLanka`)** — a GDACS event is kept if `iso3 === "LKA"`, or `country` mentions Sri Lanka, or the event's `bbox` geographically covers Sri Lanka's centroid (`7.8731, 80.7718` — the same point `SituationMap.tsx` already centers its Leaflet map on), or the title/description mentions Sri Lanka as a last resort. The bbox check matters because tropical cyclones over open ocean often have no `country`/`iso3` at all (confirmed on 2 of the 3 real TC events in the feed at research time) — country-only filtering would silently miss exactly the event type this integration was requested for.
- Frontend: a new list panel — same visual pattern as the existing DMC warnings list (event-type badge, alert-level color badge, title/name as a link, summary) — added to both `SituationMap.tsx` (admin, English-only, alongside the DMC quick-action list) and `PublicSeverityMap.tsx` (public, translated — new i18n keys `severityMap.gdacs*` added to all three locale files). Usually shows "no events" — same as DMC — since Sri Lanka rarely has an active GDACS-tracked event; that's real state, not a broken feature.
- Verified live: unit-tested `isRelevantToSriLanka` against 6 synthetic cases (iso3 match, country-text match, bbox-contains match, bbox-excludes match, title-text match, and a genuinely irrelevant Canada earthquake) — all correct, confirming the filter is actually selective rather than trivially returning empty. Cross-checked the real feed's normalized event shape against exactly what the frontend's `GdacsEvent` interface expects (field-for-field match, including `summary` from `stripHtml(item.description)`). Confirmed both `SituationMap.tsx` and `PublicSeverityMap.tsx` render the correct "no events" empty state live in the browser, matching the feed's actual current content (no Sri Lanka-relevant events at verification time).

### Cyclone track polygon + colored marker on `/admin/map`

A follow-on request: draw the storm's geometry on the existing Leaflet map, not just list it as text.

- **The RSS `<item>` itself has no geometry beyond a single current point — had to go find the real per-event geometry resource, not guess a field name.** GDACS's `/gdacsapi/api/polygons/getgeometry` endpoint (the natural guess for a "geometryUrl") is under the same `/gdacsapi/` path as the broken `geteventlist/SEARCH` API and failed with the identical connection-timeout pattern — confirmed by testing it directly, not assumed to be broken by association. What does work: every RSS item's `gdacs:cap` field links to that event's **CAP (Common Alerting Protocol) XML** file, served from a different path (`/contentdata/resources/...`, reliably reachable, confirmed 200). Its `<info><area><polygon>` element contains the storm's current hazard-area polygon as space-separated `lat,lng` pairs — already Leaflet's native `[lat, lng]` order, no coordinate-swap needed, confirmed by fetching a real cyclone's CAP file and parsing it (450 points, properly closed).
- **Important, honest caveat — this is the current episode's hazard/uncertainty-area polygon, not a literal multi-point historical track line.** GDACS's RSS only ever exposes the latest episode; the REST API that would provide episode-by-episode history is the same one that's down. So the polygon shown is "here's the storm's current affected-area shape," not "here's everywhere it's been." Worth knowing before describing this as track history anywhere it matters (e.g. the dissertation write-up).
- `server/src/routes/external.js` — new `fetchCapPolygon(capUrl)` helper, called for every Sri Lanka-relevant event's `capUrl` inside the same cached `GET /gdacs` response (cheap since there are rarely more than 0–2 relevant events at once). Fails soft to `geometry: null` per event on any error, same pattern as the rest of this file.
- `SituationMap.tsx` — each event renders a Leaflet `<Polygon>` (when `geometry` is present) and a `<CircleMarker>` at its current position, both colored by `alertLevel` using `GDACS_ALERT_LEAFLET_COLOR` — the exact same hex values (`#16a34a`/`#f97316`/`#dc2626`) already used by `SEVERITY_COLOR`/`AREA_LEVEL_COLOR` elsewhere on this page, just re-keyed by GDACS's Green/Orange/Red instead of a status string, so the map reads consistently regardless of which layer you're looking at. The marker's popup follows the same `<strong>name</strong><br/>detail<br/>detail` pattern as the existing request/donation/gauge popups: event name, `severityText` (e.g. wind speed), and a formatted from–to date range.
- **Its own fourth view-mode tab ("Global Alerts (GDACS)"), not layered on top of the other three.** The first version rendered the GDACS layer unconditionally across all view modes on the theory that a cyclone is relevant context regardless of lens — the user pushed back immediately ("its in request tab, lets have a separate one for this so it doesnt mess up"), so `ViewMode` gained a fourth `"gdacs"` value alongside `requests`/`areas`/`gauges`, matching the existing pattern exactly: its own toggle button, its own legend entries (swapped from an unconditional `gdacsEvents.length > 0` check to `viewMode === "gdacs"`), its own empty-state message, and the `<Polygon>`/`<CircleMarker>` rendering gated the same way. The Requests tab's legend and map are back to showing only request/donation markers, nothing GDACS-related bleeding in.
- Verified live end-to-end: temporarily relaxed the Sri Lanka relevance filter (`.filter(i => i["gdacs:eventtype"] === "TC")`, one real cyclone forced through) to pull a genuine event with real geometry through the full pipeline — confirmed 450 polygon points and correct `severityText`/`location` via direct API call, then confirmed in the browser: the DOM's `.leaflet-overlay-pane` contained `#16a34a`-stroked path elements (correctly matching that storm's `Green` alert level), and after panning the map out to the Pacific (via dispatched wheel events — click-based zoom controls were unreliable in this browser-automation environment, not a sign of an app bug) the actual elongated hazard-area polygon was visible on screen at the right location. Reverted the temporary filter change immediately after and confirmed the endpoint returned to its normal empty-array state, with no leftover test code.

### Global scope toggle — always something real to demo

Sri Lanka realistically has an active GDACS event on only a small fraction of days. The user flagged the obvious viva risk directly: "if was to be questioned in my viva saying nothing shows i could still show global event."

- `GET /api/external/gdacs` now takes an optional `?scope=global` query param (default stays `sri-lanka`, i.e. today's filtered behavior, unchanged for every existing caller). Global scope skips `isRelevantToSriLanka` entirely and returns every current GDACS event worldwide — typically 300+ (mostly wildfires; the live feed at verification time had 324 total across EQ/WF/FL/TC/DR). Cached separately per scope (`gdacs-events-sri-lanka` vs `gdacs-events-global`) since they're different payloads with different costs.
- **CAP polygon geometry is only ever fetched for `eventType === "TC"`, in both scopes** — fetching it for a global list that can include 280+ wildfires would be a real N+1 cost for data those event types don't have anyway (only cyclones were ever confirmed to carry a usable `<polygon>`, see above). This also slightly tightens the sri-lanka-scope behavior from the previous pass (which fetched geometry for any event type with a `capUrl`, harmless since it was never populated for non-TC events but not technically correct either).
- `SituationMap.tsx`'s GDACS tab gained a "Show all global events" checkbox, wired to a `gdacsScope` state that drives three things at once so they never drift out of sync: the map's markers/polygons, the legend's empty-state text, and the "Global Disaster Alerts" list panel below the map (shared `gdacsEvents` state — flipping the checkbox re-fetches once and every consumer updates together). The list panel caps itself to the first 20 events in global scope with a "Showing 20 of N" note, since rendering 300+ list items would be unusable; the map layer renders markers for all of them (Leaflet handles hundreds of `CircleMarker`s fine) plus polygons for whichever are cyclones.
- Verified live: toggled the checkbox on `/admin/map`'s GDACS tab and confirmed real global events rendered immediately — actual earthquakes (Canada, Japan, Peru), wildfires (Greece, Angola), correctly labeled and colored, "Showing 20 of 324 current events worldwide" note present, and 183 marker paths confirmed in the DOM (the subset of the 324 events with valid coordinates). Toggled back off and confirmed it cleanly reverted to the real (empty) Sri Lanka state with no leftover global data.

### GDACS + earthquake map layers on the public severity map

A follow-on request, worth documenting exactly how it was disambiguated since the ask itself was genuinely unclear at first. The user pasted the five `SituationMap.tsx` (admin) tab labels — Requests, Areas Affected, River Gauges, Global Alerts (GDACS), Tsunami Risk (Earthquakes) — and asked "this 3 also should be available in the page dont you think?" without saying which page or which 3. Asked a clarifying question rather than guess; the user's answer just re-pasted three of the same five labels (Requests, GDACS, Earthquakes), still not naming a target page. Given the context — GDACS and Earthquakes existed as **list panels only** on the public `/severity-map` page (not as map layers, unlike the admin map), and this project's own established precedent is the user pushing back exactly this way before (*"isnt it better to have USGS earthquake tsunami-risk indicator as another tab like global alerts?"*) — the most consistent reading was: add GDACS and Earthquakes as real map layers on the **public** severity map too, matching what admin's map already has.

**"Requests" was deliberately left out, and this was stated directly rather than silently dropped.** Admin's "Requests" tab plots exact victim pinpoint locations — publishing that on the public map would violate this project's own non-negotiable privacy design (see "Privacy and security notes"), which is exactly *why* `SituationMap.tsx` (exact pins, admin-only) and `PublicSeverityMap.tsx` (aggregates only) are two separate pages backed by two separate endpoints in the first place. The public page's "Areas Affected" tab already *is* the privacy-safe public equivalent of "Requests" — it was there already, nothing new needed.

- **`AreaSeverityMap.tsx`** (the shared component behind both `Landing.tsx`'s embedded preview and the full `/severity-map` page) gained two more `ViewMode` values, `"gdacs"` and `"earthquakes"`, reusing the exact rendering already proven on `SituationMap.tsx`: GDACS renders a `<Polygon>` (when CAP geometry exists) + `<CircleMarker>` colored by `GDACS_ALERT_COLOR` (Green/Orange/Red); earthquakes render `<CircleMarker>`s sized and colored by magnitude, with a `MapViewController` (`useMap()` + `useEffect` on `viewMode`) that pans out to the regional Sumatra-inclusive view on that tab and back to the normal Sri-Lanka view on every other tab — same recentering trick already used on the admin map, now generalized into the shared component.
- **Gated behind a new `extraLayers` prop, default `false`.** `PublicSeverityMap.tsx` passes `extraLayers` (so its four tabs show); `Landing.tsx`'s embedded preview does not (so the homepage teaser stays exactly as restrained as before — two tabs, no GDACS/earthquake clutter on a first-impression page). Both `gdacsEvents`/`earthquakes` are lazy-fetched only when their tab is first opened, matching the existing river-gauge lazy-load pattern in this same component.
- The list panels already on `/severity-map` for GDACS and earthquakes are unchanged and still there below the map — same "both a map layer and a list" pattern already established for these two feeds on the admin page.
- Verified live: all four tabs render correctly on `/severity-map`; clicking "Regional Seismic Activity" recentered the map out to the Bay of Bengal automatically and showed real gray/amber earthquake markers with accurate popups (e.g. "M 5.0 — 180 km NW of Sabang, Indonesia"); clicking back to "Global Disaster Alerts (GDACS)" correctly recentered to the normal Sri-Lanka view with the right (empty, real) state and legend; confirmed via `get_page_text` that `Landing.tsx`'s embedded preview still shows only "Areas Affected"/"River Levels" with no regression from the shared-component change; no console errors on either page.

## USGS earthquakes — regional tsunami-risk indicator

A new integration, not from the original feature list. The user did their own research first and brought findings rather than a request to investigate: confirmed the USGS earthquake API works and, with a broad Indian Ocean query, surfaces real events inside the Sumatra subduction zone — including one ~60km from Meulaboh, essentially the epicenter region of the 2004 Indian Ocean tsunami. Explicit instruction that came with it: frame this as a **regional tsunami-risk indicator**, not a "Sri Lanka earthquakes" feature — Sri Lanka itself sits on stable continental crust and barely registers seismic activity, so a Sri-Lanka-scoped version of this feed would look broken/empty almost every day.

- **Confirmed working by direct query**, same verify-before-building discipline as every other external source here: `https://earthquake.usgs.gov/fdsnws/event/1/query` (GeoJSON, no API key) tested directly with a real bounding box — returned 10 magnitude-4.0+ events over the last 30 days on the first real query, including the `tsunami` flag USGS itself attaches to potentially tsunami-generating events (a genuine bonus for this framing, not something added by this project).
- **Deliberately not scoped to Sri Lanka's own coordinates.** `server/src/routes/external.js`'s `USGS_BBOX` covers the wider Indian Ocean / Bay of Bengal / Sumatra subduction zone (`minLat: -10, maxLat: 20, minLng: 70, maxLng: 100`) — the zone that actually produced the 2004 tsunami — rather than a box drawn tightly around the island, which would be realistically empty on almost any given day.
- **`GET /api/external/earthquakes`** — public, cached 20 min (`USGS_CACHE_TTL_MS`), last 30 days, magnitude 4.0+, ordered newest-first. Each event is normalized to `{id, magnitude, place, time, depthKm, tsunami, url, lat, lng}`. Fails soft to `[]`, same pattern as every other feed here.
- **`PublicSeverityMap.tsx`** (public, translated — new `severityMap.earthquakes*` i18n keys in all three locale files) originally got a list panel only, on the reasoning that `AreaSeverityMap.tsx` only ever toggled Areas Affected/River Levels and had no multi-layer map to add a tab to — **since corrected, see "GDACS + earthquake map layers on the public severity map" below**, `AreaSeverityMap.tsx` now has GDACS and earthquake map tabs too, shared with this same list panel's data. Each list entry shows a magnitude badge (color-scaled gray/amber/red), a "Tsunami potential flagged" badge when USGS's own `tsunami` field is set, the place name linking to the USGS event page, and the time + depth.
- **`SituationMap.tsx` (admin) gets a proper 5th map tab, not just a list — corrected after initial pushback.** First version was list-only on this page too, reasoning that all these events are off Sri Lanka's coast anyway. The user pushed back directly ("isnt it better to have USGS earthquake tsunami-risk indicator as another tab like global alerts?") — right call: it's more consistent with the GDACS tab already on this page, reuses the same magnitude-color work, and the Sumatra subduction zone is actually close enough to Sri Lanka on a regional view to be visually meaningful, not just random pins in empty ocean. Added `"earthquakes"` as a 5th `ViewMode` (own toggle button "Tsunami Risk (Earthquakes)", own legend, own empty state, `CircleMarker`s sized/colored by magnitude with a popup matching the request/donation/GDACS popup pattern), keeping the list panel below the map too (same as the GDACS tab, which has both).
- **One real wrinkle: the default map view (zoom 8, centered on Sri Lanka) doesn't show the Sumatra region at all — no earthquake would ever be visible without recentering.** `react-leaflet`'s `MapContainer` only applies `center`/`zoom` on first mount, so switching tabs alone doesn't move the view. Added a small `MapViewController` component (`useMap()` + a `useEffect` keyed on `viewMode`) that calls `map.setView(...)` directly — zooms/pans out to `[8, 87]` zoom 5 (wide enough to show Sri Lanka and the whole Sumatra/Bay of Bengal region together) on the earthquakes tab, and back to the normal Sri-Lanka-only view on every other tab.
- Verified live on the public page (`/severity-map`, both English and Sinhala): real current events rendered correctly — e.g. "M 4.9 — 287 km SW of Banda Aceh, Indonesia" — with accurate magnitude badges, timestamps, and depths, and the tsunami-risk framing text renders as written in both languages (confirmed the Sinhala translation end-to-end, not just that the key exists).
- Verified live on `/admin/map` too, closing the earlier credentials gap: no admin account existed in this session, so registered a throwaway one through the actual `/register` UI (`Test Admin Verify`, role Administrator) rather than leaving it unverified. Clicked into the new "Tsunami Risk (Earthquakes)" tab and confirmed: the map recentered from the default Sri-Lanka zoom-8 view out to the wider region automatically, real gray/amber markers rendered scattered across the Bay of Bengal (correctly color-coded by magnitude), clicking one opened a popup with accurate real data ("M 4.5 — 188 km SE of Yārāda, India · 7/12/2026, 5:05:45 AM · 10 km deep" — matches the API response exactly), and switching back to the "Requests" tab correctly recentered the map to the normal Sri-Lanka-only view with no leftover zoom/pan state. Deleted the test admin account (both the Firestore `users` doc and the Firebase Auth user) via the Admin SDK immediately after, same cleanup convention used for other test data throughout this file.

## Weather (OpenWeatherMap)

The "Weather Information Integration" item listed in-scope back in Interim 1 but never actually built — flagged by the user directly against this file's own "not integrated yet" note, then confirmed to proceed.

- **`GET /api/external/weather`** — public, cached 30 min (`WEATHER_CACHE_TTL_MS`). Queries OpenWeatherMap's Current Weather API (`data/2.5/weather`) — **confirmed reachable by direct query, not guessed**: hit it with no API key and got back a real, documented `401 Invalid API key` response (not a connection failure or dead host), confirming the base URL/path are correct before any code was written against it, same verify-first discipline as every other external source in this file.
- **Queried per-city, not as a single national point.** `WEATHER_CITIES` in `server/src/routes/external.js` is eight cities spanning Sri Lanka's main regions — Colombo, Kandy, Galle, Jaffna, Trincomalee, Anuradhapura, Ratnapura, Batticaloa — covering coastal south/east/north, hill country, the dry zone, and the historically flood-prone Ratnapura basin. This matches the district-level granularity every other feature in this app already treats as the meaningful unit (severity map, active-district declarations, water-level alerts), rather than one number that can't actually represent conditions across the island.
- Each city is fetched independently and **fails soft per-city** (`{city, district, error: true}`) rather than dropping the whole response if one call has a transient blip — the frontend filters out `error` entries before rendering.
- Response is normalized to `{city, district, tempC, feelsLikeC, condition, description, icon, humidity, windSpeedMs, rainLastHourMm}`. `rainLastHourMm` is surfaced deliberately, not just temperature — heavy rain is the most disaster-relevant signal this feed can offer, and it's highlighted with its own badge in the UI rather than buried in a generic "conditions" string.
- **Needs `OPENWEATHER_API_KEY` in `server/.env`**, same category of user-provided blocker as the Maps API key and the FCM VAPID key — tied to the user's own OpenWeatherMap account, nothing to guess or placeholder around. Unlike Maps, **OpenWeatherMap's free tier needs no credit card** and comfortably covers this app's usage (8 cities × 48 polls/day at the 30-min cache TTL ≈ 384 calls/day, well inside the free tier's daily allowance). Without the key, the endpoint logs why and returns `[]` rather than erroring.
- Frontend: a full weather grid on **`PublicSeverityMap.tsx`** (public, translated — new `severityMap.weather*` i18n keys in all three locale files), placed right after the map and before the DMC warnings list since current conditions are the most "at a glance" of the panels on that page — 8 city cards with icon (OpenWeatherMap's own icon CDN, no icon set bundled), temperature, description, and a rain badge when `rainLastHourMm > 0`. A compact version — icon + city + temp only, no rain badge or description — sits on **`Landing.tsx`** as a horizontal strip inside the existing "Affected Areas" section, above the map preview.
- **Verified as far as this dev environment allows without the API key.** Confirmed the endpoint itself is live and correctly shaped by direct query (real `401` response, not a guess); confirmed the server starts cleanly with the new route and `GET /api/external/weather` returns `200` with `[]` plus a clear server-log line explaining why, when the key is unset; confirmed both `PublicSeverityMap.tsx` and `Landing.tsx` render their correct empty/hidden states with no console errors when the feed is empty. **Not yet verified: real weather data actually rendering** (city cards, icons, the rain badge) — needs a real `OPENWEATHER_API_KEY`, not yet provided. Do a real curl test against the live endpoint once it's in place, the same verify-before-trusting pattern used everywhere else in this file, before treating the response shape as fully proven rather than "matches OpenWeatherMap's long-stable, well-documented contract."

## Push notifications (FCM)

Requested as "SMS alerts on every move" (request accepted, delivery started, delivery ended) plus area-wide disaster alerts to victims based on their location. **Real SMS was already ruled out and documented as a scope cut** (`## Known scope cuts` below — Twilio needs a paid/verified business account). Confirmed with the user directly rather than silently building either option: they picked **Firebase Cloud Messaging** (free, browser/app push) over real SMS.

- **`server/src/utils/notifications.js`** — `sendNotificationToUser(uid, {title, body, data})`, the one shared helper every trigger point below calls. Looks up `users/{uid}.fcmTokens` (an array — one user can have several devices/browsers registered), sends via `admin.messaging().sendEachForMulticast()`, and prunes any token FCM reports as dead. **Two dead-token error codes matter, not just one** — found by testing directly rather than guessing: `messaging/registration-token-not-registered` / `messaging/invalid-registration-token` (a token that was once valid and has since expired/been uninstalled) and, separately, `messaging/invalid-argument` (FCM's actual code for a malformed/non-token string — confirmed by sending a deliberately garbage token and inspecting the real response). Fails soft everywhere — a missing profile, zero tokens, or a send error never breaks the caller's actual work (verifying a request, updating a delivery).
- **`POST /api/users/fcm-token`** (any authed user) — body `{token}`, appends to `fcmTokens` via `arrayUnion` (re-registering the same token is a no-op). No new Firestore rule needed — `users/{userId}` already allows the owner to write their own doc.
- **Three trigger points, interpreting the literal ask onto this system's actual delivery lifecycle** (documented here in case a future session wonders why these three and not others): "request accepted" → `PATCH /api/requests/:id/verify` and `/bulk-verify` when `approve === true` (this is the victim's first "good news" moment, not the volunteer's own delivery-assignment accept/reject, which is a separate concept); "started delivery" → `PATCH /api/deliveries/:id/status` when the new status is `picked_up`; "end of delivery" → the same route when status is `delivered`, **and** `PATCH /api/deliveries/:id/self-deliver` (self-delivery skips `picked_up` entirely, so it only ever fires the `delivered` copy). Deliberately notifies at `delivered` ("scan to confirm") rather than `confirmed` — the victim is the one who triggers `confirmed` themselves by scanning, so telling them what they just did would be redundant; the useful moment to interrupt them is when the QR is ready to scan, not after.
- **Frontend (`web/src/lib/firebase.ts`, `lib/notifications.ts`, `components/NotificationSetup.tsx`, `public/firebase-messaging-sw.js`):** `firebase.ts` exports `messagingPromise` (an `isSupported()`-gated promise, since `getMessaging()` throws outright in browsers without Push API support — Safari <16.4, non-HTTPS/non-localhost — rather than returning `null` the way the rest of this SDK does). `NotificationSetup.tsx` is rendered once in `DashboardLayout.tsx` (so every logged-in role gets it, not just victims — no reason to restrict it given volunteers/donors could get their own notification types later) and handles the whole lifecycle: silently re-registers the token on every login if permission was already granted in a past session, shows a one-line dismissible opt-in bar if permission hasn't been decided yet (`Notification.permission === "default"`), and renders a toast for foreground messages (background messages are handled by the service worker instead — FCM never calls `onMessage` for a page that's frontmost, only `onBackgroundMessage` in the SW for everything else).
- **`public/firebase-messaging-sw.js` must live at the site root** (FCM's hard convention, not configurable) and, being a plain script the browser loads directly rather than something Vite bundles, **can't read `import.meta.env`** — its Firebase config values are hand-duplicated from `web/.env` with a comment explaining why. This is fine, not a leak: these are the same public client identifiers already documented as "not secret" in `firebase.ts`, and they already ship inside the built JS bundle sent to every visitor regardless.
- **Needs a Web Push certificate (VAPID key) the user has to generate themselves** — Firebase Console > Project Settings > Cloud Messaging tab > Web configuration > "Web Push certificates" > generate a key pair — same category of blocker as the Google Maps API key (tied to their own Firebase project, nothing to guess or placeholder around). Added `VITE_FIREBASE_VAPID_KEY=` to `web/.env` with a comment; `requestAndRegisterFcmToken()` logs a clear console warning and returns `"unsupported"` rather than throwing if it's missing.
- **Verified as far as this dev environment allows, honestly split between what's proven and what isn't:**
  - Proven end-to-end via real HTTP calls (not just unit-testing the helper in isolation): registered a throwaway victim through the actual `/register` UI, then — since this browser automation environment reports `Notification.permission` as permanently `"denied"` (a sandbox/headless policy, not an app bug — same category of limitation already documented for camera QR scanning and geolocation) — obtained real Firebase ID tokens for temporary admin/volunteer test accounts via the Identity Toolkit REST API (`accounts:signInWithPassword`) and called the actual Express routes directly: `PATCH /:id/verify` (approve), `PATCH /:id/status` (`picked_up` then `delivered`). All three returned 200 with no server-side errors, confirming the notification call sites are wired correctly (right field names, right imports, no crashes) even though no real device ever received a push.
  - Proven directly against `sendNotificationToUser` itself: a user with zero `fcmTokens` no-ops silently; a registered token that fails to send gets pruned from Firestore automatically; both of FCM's distinct dead-token error codes are handled (found the second one, `invalid-argument`, only by testing with a real malformed token and inspecting the actual response rather than assuming one error code covered every failure mode).
  - **Not proven, and can't be from this environment:** the actual browser permission-prompt UI, a real device receiving a real push notification, and the service worker's background-message handler firing. All three need the VAPID key (not yet provided) plus a real browser with notification permission actually grantable — worth a manual phone/browser check once the VAPID key is in place, the same caveat already used elsewhere in this file for things this tooling genuinely cannot exercise.
  - All test accounts, the test request, and the test donation/delivery created for this verification were deleted afterward via the Admin SDK, same cleanup convention used throughout this file.

## Water-level area alerts

The second half of the "SMS alerts" ask: notify victims in a specific area when it's disaster-relevant — the concrete example given was "rise if water levels." Came with an explicit constraint the user settled directly rather than leaving to a default: *"lets keep both options open, approved by admin and send automatically in the admin panel with a toggle so admin can choose to automatically send or no"* — so this isn't just the auto-vs-manual choice made once at build time, it's a live admin-facing toggle.

- **`server/src/utils/waterLevelAlerts.js`** is the whole feature's brain. `STATUS_SEVERITY` ranks `normal < alert < minor_flood < major_flood`; an alert only ever fires on a genuine **escalation** (severity strictly increasing since the last check), never on de-escalation and never on staying at the same level — a station stuck at "alert" for hours doesn't spam the same victims every 10 minutes. Verified directly: re-running the check with an unchanged status produced zero new `sentAreaAlerts` entries.
- **`waterLevelAlertState`** (one doc per gauge station, doc id = slugified station name) stores just `{station, lastStatus, updatedAt}` — the minimum needed to detect "did this get worse since last time," updated on every poll regardless of whether an alert fired.
- **District matching reuses the exact same `nearestDistrict()` function** every other district-aware feature in this app already uses (stats-by-area, the requests filter, active-district declarations) — "which victims are in this district" means the same thing everywhere, not a second slightly-different implementation. A district match pulls every victim with an active (`pending`/`verified`/`in_progress`) request whose `location` resolves to that district, deduped by victim.
- **The admin toggle is a single Firestore doc** (`waterLevelAlertSettings/config`, `{autoSend: boolean}`), read fresh on every poll cycle — flipping it in the admin UI takes effect on the very next check, no restart needed. **Defaults to `false` (review-first) when the doc doesn't exist yet** — the safer of the two options is what a fresh install gets without anyone having to remember to configure it.
  - `autoSend: true` → `sendAreaAlert()` runs immediately: notifies every matching victim via the FCM helper from `## Push notifications` above, and logs one `sentAreaAlerts` entry (`source: "auto"`).
  - `autoSend: false` → a `pendingAreaAlerts` doc is created instead (nothing sent yet). `AdminWaterAlerts.tsx` (`/admin/water-alerts`) lists these with **Approve & send** / **Reject** buttons — approving calls the exact same `sendAreaAlert()` function (`source: "admin_approved"`, `approvedBy` recorded), so there's only one code path that actually sends a notification, not two slightly-different ones to keep in sync. Both approve and reject are logged to the audit log (`area_alert.approve` / `area_alert.reject`, extending `AuditLog.tsx`'s existing `ACTION_LABELS`/`describeEntry`).
- **A `setInterval` poller in `server.js`** calls `checkWaterLevelsAndAlert()` every 10 minutes, plus once immediately at startup (same warm-up pattern already used for the HDX district-boundaries cache) — this is what actually detects a rising water level; it doesn't wait for anyone to load a map. It reuses `external.js`'s existing `fetchWaterLevels()` (extracted into a standalone function and attached to the exported router as `router.fetchWaterLevels`, since Express Router instances are just functions and can carry extra properties) rather than making a second HTTP call to the same river-gauge feed the map already polls — same cache, same data, no extra load on the unofficial upstream proxy.
- **New endpoints, all admin-only**: `GET`/`PATCH /api/water-alerts/settings` (the toggle), `GET /api/water-alerts/pending`, `POST /api/water-alerts/pending/:id/approve`, `POST /api/water-alerts/pending/:id/reject`, `GET /api/water-alerts/sent` (last 50, history). No new Firestore rules needed — `AdminWaterAlerts.tsx` uses one-time `apiFetch` calls through these Express routes, not a direct client-SDK `onSnapshot` read, so nothing here is reachable by a signed-in client bypassing the API (consistent with the "lower value to make live" reasoning already used for category/broadcast history views).
- **Verified end-to-end against real infrastructure, but with one honestly-labeled synthetic input.** All 41 real river gauge stations were genuinely at `normal` status at verification time (confirmed by hitting the live feed directly) — there was no real flood to test an escalation against, the same "nothing to demo" situation already hit with GDACS and solved the same way: use real machinery, be upfront that the *trigger* was simulated. Monkey-patched the shared `fetchWaterLevels` export with one clearly-named `"TEST STATION (simulated)"` entry and ran the real `checkWaterLevelsAndAlert()`:
  - With `autoSend: false` (the default): a real `pendingAreaAlerts` doc was created, correctly matched to "Colombo" district via the real `nearestDistrict()` call. Confirmed via the real `GET /api/water-alerts/pending` HTTP route with a genuine admin ID token.
  - Approved it via the real `POST /api/water-alerts/pending/:id/approve` route — `notifiedCount: 3` (correctly found 3 real victims with active Colombo-area requests already in the database, not just the one throwaway test victim created for this check), moved to `sentAreaAlerts` with `source: "admin_approved"`.
  - Flipped `autoSend` to `true` via the real `PATCH /api/water-alerts/settings` route, then escalated the same test station further (`alert` → `major_flood`) — this time it sent immediately with **no** pending item created, confirming the branch actually depends on the live toggle value, not a cached one.
  - Re-ran the identical `major_flood` status a third time and confirmed zero new `sentAreaAlerts` entries — the no-repeat-spam guarantee holds.
  - Also click-verified `AdminWaterAlerts.tsx` itself in the browser (registered a throwaway admin through `/register`): the toggle button flips between "Auto-send: Off"/"Auto-send: On" via real clicks against the real endpoint, and the empty states render correctly.
  - All test accounts, the test request, the test station's `waterLevelAlertState` doc, and the test `sentAreaAlerts` entries were deleted afterward, and `autoSend` was reset to `false` (the safe default) to leave real operation conservative.

## SOS / rescue flow

Explicitly requested as its own thing, separate from aid requests: *"'trapped,' 'missing persons,' and 'flood rescue' are life-safety emergencies needing immediate dispatch, fundamentally different urgency from 'I need food.'"* Three real decisions were confirmed with the user before building rather than guessed, since each has real security/UX consequences: **SOS requires login** (reuses the existing auth model rather than opening public unauthenticated writes), **dispatch means status tracking only** (`pending → acknowledged → in_progress → resolved`, not shoehorned into the volunteer/delivery-assignment pipeline — coordinating a rescue isn't "deliver goods"), and **any authenticated role can report one**, not just "victim" (someone could be reporting on behalf of a person they've spotted in danger).

- **`sosRequests`** is a completely separate Firestore collection from `aidRequests` — no shared schema, no priority-score algorithm, no category caps. Fields: `{reporterId, reporterName, reporterPhone, type: "trapped"|"missing_person"|"flood_rescue"|"other", peopleCount, description, location, status, createdAt, updatedAt, acknowledgedAt, resolvedAt}`.
- **`POST /api/sos`** (any authed role) — deliberately minimal required fields (`type` + `location` only; `peopleCount`/`description` optional) so submitting is as close to one tap as this app gets. Every admin is pushed an FCM notification immediately (`sendNotificationToUser` from `## Push notifications` above, fanned out to every `role: "admin"` user) — this is the one place in the app where "someone sees this in seconds" is the actual requirement, not "eventually shows up in a queue."
- **`PATCH /api/sos/:id/status`** (admin) — the four-stage status flow, stamping `acknowledgedAt`/`resolvedAt` once (same "set on first transition, never overwritten" pattern as `aidRequests.verifiedAt`). Notifies the original reporter on every status change too, so they know help is coming without refreshing anything. Logged to the audit log (`sos.status_update`, extending `AuditLog.tsx`).
- **`SosButton.tsx`** — a persistent floating red button rendered in `DashboardLayout.tsx` for every logged-in role, opening a single-screen modal (not a multi-step wizard, which would add friction, not remove it): 4 big type buttons, geolocation auto-captured the instant the modal opens (not gated behind a second "capture location" tap), optional people-count and description fields, one submit button. Fully translated (EN/SI/TA) since this is exactly the kind of high-stakes, high-equity surface the i18n work has prioritized elsewhere.
- **`SosStatusBanner.tsx`** — a live `onSnapshot` listener (own component, rendered in `DashboardLayout.tsx` alongside `EmergencyBanner`) showing the reporter their own unresolved SOS status at the top of every page, colored by urgency. This is arguably the single highest-value live listener in the app — someone who just called for help should see it's being handled without hunting for a page.
- **`AdminSos.tsx`** (`/admin/sos`, top of the admin nav — deliberately above Aid Requests, since this is the most time-sensitive queue in the app) — a live `onSnapshot` dispatch board: a small Leaflet map with colored markers (red pending / amber acknowledged / blue in-progress) plus a list with one-click "Acknowledge" → "Start response" → "Mark resolved" buttons per report, unresolved sorted to the top always. Resolved reports collapse into a small history section below rather than disappearing.
- **New `sosRequests` Firestore rule block added** (`create`: any signed-in user; `read`: the reporter or an admin; `update`: admin only) — this one, unlike the water-level-alerts collections, genuinely needs rules since both `AdminSos.tsx` and `SosStatusBanner.tsx` read it directly via `onSnapshot`, not through the Express API.
- **Verified against the real backend end-to-end via genuine HTTP calls** (temp victim + temp admin accounts, real Firebase ID tokens via the Identity Toolkit REST API, same method already established in this file): a real `POST /api/sos` created a `pending` report; `GET /api/sos` (admin) and `GET /api/sos/mine` (victim) both returned it correctly; a victim attempting `PATCH .../status` correctly got 403; the admin progressed it through `acknowledged → in_progress → resolved`, with all three transitions correctly logged to the audit log with accurate details. All test data deleted afterward.
- **Not yet verified: the two live `onSnapshot` surfaces (`AdminSos.tsx`, `SosStatusBanner.tsx`) in an actual browser.** The updated Firestore rules need to be deployed first (`firebase deploy --only firestore:rules`) — asked the user for that confirmation and haven't heard back yet, so deployment was deliberately held rather than assumed. Everything server-side is proven correct; what's unverified is purely "does the live listener render once the rules allowing it are actually live," which is a much smaller remaining risk than the backend logic itself.

## Community reports (road closures / water conditions)

The second half of the same request as SOS, framed as a specific critique: *"connecting reports to action"* is the crowdsourcing gap the user's own research already identified in Ushahidi — a platform that collected reports but didn't visibly close the loop back into response. Confirmed with the user directly: **submissions restricted to volunteers only** (a smaller, higher-trust pool than opening it to every role, trading coverage for less moderation burden), not victims or donors.

- **`communityReports`** Firestore collection: `{reporterId, reporterName, type: "road_closure"|"water_level"|"other", description, location, district (computed via nearestDistrict() at submission time, same as everywhere else), status: "unverified"|"verified"|"dismissed", createdAt, updatedAt, verifiedAt, verifiedBy}`.
- **`POST /api/community-reports`** (volunteer-only). **`GET /api/community-reports`** (admin, unverified sorted first). **`GET /api/community-reports/verified`** (public, no auth) — the one and only thing the public ever sees; an unverified or dismissed report never reaches anyone outside the admin review queue, so a false or mistaken report can't spread before a human checks it.
- **The actual "connects to action" mechanism, not just a status flip nobody sees: `PATCH /api/community-reports/:id/verify`** takes an optional `activateDistrict: boolean` alongside `approve`. Checking it when verifying writes straight into the **same `activeDistricts` collection** the DMC-alert quick action and `/admin/active-emergencies` already use — a verified volunteer report of "bridge flooded near Kelaniya" can, in the same click, declare that district an active emergency, which immediately feeds the public banner and the victim request form's soft note (see "Active emergency districts" above). This is the concrete, demoable answer to "how does a report actually change anything," reusing infrastructure rather than building a second parallel system.
- **`AdminCommunityReports.tsx`** (`/admin/community-reports`) — unverified reports on top with Verify/Dismiss buttons and the "also declare an active emergency" checkbox, reviewed ones below. One-time `apiFetch`, not `onSnapshot` — same "lower urgency than SOS, doesn't need to be live" reasoning as the water-alerts admin page. **No new Firestore rule needed** — nothing here is read directly by a client via the SDK.
- **Submission UI lives inside `VolunteerDeliveries.tsx`** (a form section at the bottom of the volunteer's existing main page) rather than a new nav item — a volunteer is already there checking their delivery queue, and this is routine reporting, not an emergency needing its own always-visible entry point the way `SosButton` does.
- **Verified reports surface in two places**, both reusing the existing DMC/GDACS/earthquake list-panel pattern: `PublicSeverityMap.tsx` (translated EN/SI/TA, new `severityMap.communityReports*` i18n keys) and `SituationMap.tsx` (admin, English-only, with a link back to the review page).
- **Verified end-to-end via real HTTP calls** (temp volunteer + temp admin, real Firebase ID tokens): submitted a real report — confirmed it correctly resolved to "Colombo" via `nearestDistrict()` and stayed invisible on `GET /api/community-reports/verified` while unverified; confirmed a volunteer gets 403 on the admin-only list endpoint; approved it with `activateDistrict: true` — confirmed it then appeared on the public verified-reports endpoint **and** that Colombo appeared in `GET /api/active-districts` with `sourceAlertTitle: "Community report: Bridge near Kelaniya flooded, impassable"`, proving the promotion path is real, not just wired-looking. Both `community_report.verify` and the resulting `district.activate` were correctly logged to the audit log. **One honest note on cleanup:** Colombo was already an active district from earlier, unrelated verification work in this project's history before this test touched it — my write refreshed its `activatedAt`/source metadata but didn't change whether it was active (active before, active after), so it was left as-is rather than deactivated, to avoid reverting state this session didn't originally set. All test accounts and the test report itself were deleted afterward.

## QR-code delivery confirmation

Replaces a same-device "Confirm receipt" button tap — the victim now has to physically scan a code shown by whoever has the goods, which is harder to fake.

- When a delivery reaches `delivered` (via `PATCH /:id/status` or `PATCH /:id/self-deliver`), the backend generates a random `confirmToken` (`crypto.randomBytes(16)`) and stores it on the delivery.
- `DeliveryQrCode.tsx` renders that token + the delivery id as a QR (`qrcode.react`) on `VolunteerDeliveries.tsx` and `DonorMyDonations.tsx` (self-delivery) — shown to whoever has physical custody of the goods.
- `QrScanModal.tsx` opens the victim's camera (`html5-qrcode`) from `VictimMyRequests.tsx`'s "Scan to confirm" button, decodes `{deliveryId, token}`, and calls `POST /:id/confirm` with that token.
- **`POST /api/deliveries/:id/confirm` now requires the body's `token` to match `delivery.confirmToken`** in addition to the existing victim-ownership check — a wrong or missing token gets a 403, verified live.
- `GET /api/deliveries/by-request/:requestId` (the victim's own delivery list) deliberately **strips `confirmToken` out of the response** — the victim's client is never handed the token directly, only whoever generates the QR sees it. Don't add it back there even if it seems convenient.
- Camera access needs a secure context (HTTPS) in production — works on `localhost` for dev without issue, but note this when deployment planning starts.

**Note on live location:** `currentLocation` today is only a snapshot captured once per status-change button click, not continuous tracking. True "PickMe-style" live tracking (needed for turn-by-turn navigation) needs `navigator.geolocation.watchPosition` streaming into Firestore periodically, with the victim/admin side using a real-time Firestore listener instead of the current polling `apiFetch` pattern. Bigger lift than it sounds — its own task when navigation gets built, not a small addition.

## Maps — built

Three map surfaces, all live:
- `SituationMap.tsx` (`/admin/map`, admin-only) — three-way toggle: "Requests" (exact pinpoint markers for every aidRequest colored by severity + donation pickup points, from the existing admin list endpoints), "Areas Affected" (district aggregates, same data as the public map), "River Gauges" (live gauge stations colored by flood-threshold status). Also lists recent DMC emergency warnings below the map.
- `AreaSeverityMap.tsx` (shared component, `web/src/components/`) — toggle between "Areas Affected" (district-level circles, count + avg severity → none/low/moderate/high, from `GET /api/stats/by-area`) and "River Levels" (live gauge stations from `GET /api/external/water-levels`, colored normal/alert/minor flood/major flood). An `extraLayers` prop (default off) adds two more tabs, "Global Alerts (GDACS)" and "Tsunami Risk (Earthquakes)" — on for `/severity-map`, off for the Landing preview, see "GDACS + earthquake map layers on the public severity map" above. Never receives or renders a per-victim coordinate — everything here is either aggregated or public infrastructure data. Used by both:
  - `Landing.tsx` — embedded preview (420px), popups disabled (`showPopups={false}`) for a cleaner glanceable view, links to the full page
  - `PublicSeverityMap.tsx` (`/severity-map`, public, no login) — full-size (600px), popups enabled, plus the DMC emergency warnings list below it
- District attribution (for "Areas Affected") uses **nearest-centroid matching** against a hardcoded list of Sri Lanka's 25 district centroids (`server/src/utils/districts.js`), not real polygon boundaries — a deliberate simplification since no boundary GeoJSON was available and an external geocoding API would add a dependency/cost for something free-tier OSM tiles don't need. Points near a district border can get attributed to the neighboring district. Swap for real boundaries if precision ever matters.

### Real district boundary polygons (HDX COD-AB)

A follow-on request: replace the centroid-circle approximation on the public severity map with real district polygons, from HDX's official COD-AB (Common Operational Dataset — Administrative Boundaries) dataset — Survey-Department-of-Sri-Lanka-sourced, OCHA-vetted, admin levels 0-4 (country → 9 provinces → 25 districts → 339 DS divisions → 14,043 GN divisions). Admin-2 (25 districts) is what matches this project's existing district granularity.

- **The specific "live ArcGIS geoservice" the user asked for is genuinely dead — verified, not assumed.** `codgis.itos.uga.edu` and `gistmaps.itos.uga.edu` (the documented COD_External ArcGIS REST hosts) return **NXDOMAIN from both Google's (8.8.8.8) and Cloudflare's (1.1.1.1) public DNS** — not a local network fluke, a real global DNS failure. ITOS (Information Technology Outreach Services, University of Georgia — who ran this service) has itself been reorganized: their old site now 301-redirects into UGA's Carl Vinson Institute of Government. Flagged to the user directly (via AskUserQuestion) rather than silently substituting, same pattern as the DMC and GDACS findings above.
- **What's actually reachable: HDX's own file CDN.** `data.humdata.org`'s CKAN API (confirmed working, `curl`-tested directly) serves the same official COD-AB dataset as a downloadable resource, redirecting to an S3-backed URL. Downloaded and inspected directly: `lka_admin2.geojson` has exactly 25 features, real Survey-Department polygons, and — a genuine bonus — English/Sinhala/Tamil district name fields already included (`adm2_name`/`adm2_name1`/`adm2_name2`), on top of province, p-code, and area.
- **The raw file is too heavy to serve to a browser as-is** (~20MB for just the admin-2 layer — precision-grade GIS data, not web-map-ready) and the full zip (all 5 admin levels + lines/points) is ~130MB. `server/src/routes/external.js`'s `fetchDistrictBoundaries()` downloads the zip, extracts only `lka_admin2.geojson` via `adm-zip`, and simplifies it with `@turf/simplify` (Douglas-Peucker, `tolerance: 0.002`, `highQuality: true`) — cuts it from ~20MB / hundreds of thousands of vertices down to **~280KB / 6,749 points** across all 25 districts, visually indistinguishable at web-map zoom levels. Response fields are trimmed/renamed to `{district, districtSi, districtTa, province, pcode, areaSqKm}` per feature.
- **`GET /api/external/district-boundaries`** — public, cached 24h (`DISTRICT_BOUNDARIES_CACHE_TTL_MS`, the longest TTL of any external feed here — district boundaries don't change during a dissertation's lifetime). Fails soft to `null`, same as every other external feed.
- **The cold-cache download alone takes 1-2.5+ minutes** (133MB over a real network) — far too slow to happen inside a page load. Two mitigations, both in `external.js`: (1) the cache is **warmed once at server startup** — a fire-and-forget `getCached(...)` call at module load, so by the time a real user (or a viva examiner) opens the map, the fetch has almost certainly already completed in the background; (2) the frontend (`AreaSeverityMap.tsx`) fetches boundaries in a **separate, non-blocking `useEffect`** that never gates the map's initial render — if `boundaries` is still `null` (cold cache mid-warm-up, or the fetch failed), the map **falls back to the original centroid-circle rendering** exactly as before, so the feature can only improve the map, never break it.
- `AreaSeverityMap.tsx` renders district polygons via react-leaflet's `<GeoJSON>` (colored via a `style` function that looks up each polygon's district name against the existing `/api/stats/by-area` response — same severity levels, same color values, just filled into a real shape instead of a circle) with `onEachFeature` binding the same popup content as before. Districts with **no active requests get a distinct "No activity" gray** (`#9ca3af`, low fill opacity) rather than being invisible or reusing the "low" green — added a 4th legend entry and a new `severityMap.noActivity` i18n key (EN/SI/TA) for it, since without it a mostly-quiet demo dataset would render 24 of 25 districts as blank space.
- Verified live: `curl`'d the endpoint directly (25 features, correct trimmed properties, Sinhala/Tamil names intact); confirmed in-browser via DOM inspection that all 25 `<path>` elements render with the correct fill (1 district colored by its real active-request level, 24 in the "no activity" gray); clicked the colored district's popup and confirmed accurate content ("Colombo — 3 active requests — High need") rendered over the district's actual non-circular shape, not a circle; confirmed the same shared component still works correctly on both the Landing page's embedded preview and the full `/severity-map` page, and that the "River Levels" tab (still `CircleMarker`-based, unaffected by this change) continues to work.

## Internationalization (English / Sinhala / Tamil)

`i18next` + `react-i18next`, language auto-detected then persisted to `localStorage` (`i18nextLng` key), switchable via `LanguageSwitcher.tsx` (shown on Landing, Login, Register, TransparencyDashboard, PublicSeverityMap, and in the DashboardLayout sidebar for logged-in pages).

**Coverage as of this handoff:** Landing, Login, Register, DashboardLayout nav, VictimRequestForm, VictimMyRequests, TransparencyDashboard, PublicSeverityMap, AreaSeverityMap, StatusBadge, Footer — the highest-equity-impact surfaces (public-facing + victim-facing), prioritized deliberately over admin/donor/volunteer internal pages (AdminRequests, AdminDonations, VolunteerDeliveries, DonorDonationForm, DonorMyDonations), which are **still English-only**. Extend those the same way if asked: `useTranslation()`, `t("namespace.key")`, add the key to all three `locales/*.json` files.

**Translation quality — read before treating these as final:** the Sinhala and Tamil strings are AI-generated best-effort, not reviewed by a native speaker. Reasonable for a dissertation demo of the capability and for the equity/accessibility argument in the research questions, but **get them checked by a native Sinhala/Tamil speaker before any real deployment** — mistranslation in disaster-relief communication is a real harm, not a cosmetic issue. This caveat is also in `i18n.ts`'s file comment and `LanguageSwitcher.tsx`'s comment; keep it there.

## Visual design pass

Landing page redesigned to feel less generic: sticky header with a scroll-triggered shadow, subtle blurred-blob hero background (pure CSS, no images) with a wavy SVG divider, a live stats bar (`StatCard.tsx`, real numbers from `GET /api/stats`), a "How It Works" 3-step section with `lucide-react` icons, scroll-reveal fade-ins (`ScrollReveal.tsx`, IntersectionObserver, no animation library), icons on the role cards, and a proper `Footer.tsx` (platform links + DMC/Irrigation Dept data-source credit). Kept deliberately restrained — one accent color, no parallax, no heavy animation — since this is a disaster-relief site where "calm and credible" matters more than flashy.

`GET /api/stats` gained two fields to support the stats bar: `totalVolunteers` (count of users where role="volunteer") and `districtsReached` (distinct `nearestDistrict()` values among all requests with a location) — both also shown on TransparencyDashboard now.

### Color/palette reskin (deep slate blue + emergency orange)

A follow-on request, superseding the "one accent color" restraint described just above: the user supplied an AI-image-generation-style prompt ("modern... deep slate blue and emergency orange accent color palette... soft rounded cards...") and asked for the whole app to be reskinned to match, with an explicit "if I don't like it, change it back" condition. **No git commits exist in this repo** (confirmed via `git log` — `fatal: ... does not have any commits yet`), so there was no commit-based rollback available; the change was instead planned as a small number of mechanical, reversible find-and-replace patterns so it could be hand-reverted file-by-file if ever needed.

- **The core move: blue was freed up to mean "info/status" only, once the brand/action color moved to orange.** Grepped every `blue-*` usage across `web/src` first and found it was already cleanly split into two non-overlapping uses — (a) brand/structural color (solid CTA buttons, active-tab/nav-selection states, text links) and (b) semantic status color (`StatusBadge.tsx`'s delivery-status pills, SOS/broadcast/water-alert banner tints, GDACS "normal" badges, audit-log badges) — so recoloring (a) to orange left (b) untouched and unambiguous, with zero manual disambiguation needed file-by-file.
- **Mechanical, scoped sed passes across `web/src/**/*.tsx`, not a manual per-file rewrite:** `bg-blue-600`/`hover:bg-blue-700` → `bg-orange-600`/`hover:bg-orange-700` (26 solid-button/active-tab instances); `text-blue-600 hover:underline` and its two class-order variants → `text-slate-700 hover:underline` (text links only — confirmed via grep that every non-link `text-blue-600` usage was exactly 4 instances, all in `Landing.tsx`, handled by hand instead); `rounded-lg` → `rounded-xl` app-wide for "soft rounded cards" (safe globally because buttons use plain `rounded` and pills use `rounded-full` — confirmed no class collision before running it, so this one didn't need per-file scoping either).
- **Two "selected choice card" spots initially missed by the button/link greps, caught by a second full-inventory grep afterward:** `DonorDonationForm.tsx`'s delivery-method picker and `VictimRequestForm.tsx`'s category picker both used `border-blue-600 bg-blue-50 text-blue-700` for their selected state — visually a selection indicator, not a status badge, despite using the same color combination as one — updated to the orange equivalent by hand.
- **`Landing.tsx` hand-edited beyond the mechanical passes:** hero gradient background `from-blue-50` → `from-slate-50`; the two decorative blurred hero blobs recolored from blue/indigo to slate/orange; the "STEP 1/2/3" uppercase accent label → orange; the three role-card icons (`LifeBuoy`/`Gift`/`Truck`) → slate (structural/informational, not action, so kept off the orange reserved for CTAs).
- **`DashboardLayout.tsx` sidebar got a structural change, not just recoloring** — this is the one place the "deep slate blue... dashboard chrome" part of the brief needed more than a find-and-replace: background flipped from white to `bg-slate-900`, nav text to `text-slate-300`/`hover:text-white`, the active-nav-item highlight changed from `bg-blue-50 text-blue-700` to solid `bg-orange-600 text-white`, and the bottom profile/logout block recolored to match (`bg-slate-800` logout button). Every other page's layout structure (cards, forms, tables, modals) was left alone — only colors and border-radius changed there.
- Verified via `npx vite build` (clean, no new errors — the handful of pre-existing `tsc --noEmit` errors around `import.meta.env` typing and one `Earthquake` lat/lng field predate this change and are unrelated) and live in the browser: Landing page (hero, "How It Works" icons, map view-toggle tabs, role cards, footer), the login/register auth cards, and the admin dashboard (sidebar, KPI cards, Aid Requests table with priority badges) — all rendering with the new palette, semantic status badges (priority score pill, delivery-status pills) confirmed untouched and still their original colors. Registered a throwaway admin account through the real `/register` UI to check the logged-in sidebar/dashboard view, then deleted it (Firestore doc + Firebase Auth user) via the Admin SDK afterward, same cleanup convention used throughout this file.
- **If the user says they don't like it**: every change above is a small number of scoped, greppable patterns (`orange-600`→`blue-600`, `rounded-xl`→`rounded-lg`, the `DashboardLayout.tsx`/`Landing.tsx` hand-edits reversed individually) — there is no git commit to fall back on, so reverting means re-applying the inverse of each bullet above, not a `git checkout`.

## Mobile app (Expo / React Native)

The user asked to start the mobile app, chose **full parity with web** (every role including admin, eventually) as the scope, and **Expo Go on their own phone** (not an emulator) as the test device. Given the size of that scope, being built the same way this whole project has been — one vertical slice at a time, each verified before moving to the next — rather than everything at once. `mobile/` is a fresh Expo TypeScript app (blank-typescript template), separate from `web/` and `server/`.

**Done so far: navigation shell, auth, and the full victim flow.** Donor/volunteer/admin screens exist as `ComingSoon` placeholders (`mobile/src/components/ComingSoon.tsx`) so the app is fully runnable end-to-end right now, just with those roles' actual screens not built yet — swap each placeholder out as its task comes up.

- **Firebase JS SDK, not React Native Firebase.** Same Firebase project as web, same config values (`mobile/.env`, `EXPO_PUBLIC_FIREBASE_*` — Expo auto-inlines any `EXPO_PUBLIC_` prefixed var from `.env` at build time, no extra config needed, unlike web's Vite-specific `import.meta.env`). Chosen because it works inside plain **Expo Go** with zero native build step — React Native Firebase needs a custom dev client (`expo prebuild` + EAS build), which would have blocked the "test in Expo Go" requirement from the start.
- **`mobile/src/lib/firebase.ts`** uses `initializeAuth(app, { persistence: getReactNativePersistence(AsyncStorage) })` instead of web's plain `getAuth()` — without this, a logged-in user gets signed out every time the app restarts, since RN has no browser storage for Firebase Auth to fall back to automatically.
- **A real, documented TypeScript quirk, not a runtime bug:** `getReactNativePersistence` lives in `firebase/auth`'s React-Native-specific build, which **Metro** resolves correctly (via the package's legacy `"react-native"` main-field, which Metro understands and bare `tsc` doesn't), but plain `tsc --noEmit` only sees the browser build's types and reports it as missing. Fixed with a module-augmentation declaration in `mobile/env.d.ts` (needs `export {}` at the top of that file to force module — not global-script — mode, or the augmentation silently *replaces* firebase/auth's real types instead of merging with them — hit this exact failure once while building it, worth remembering if this pattern is ever copied elsewhere). The same file also declares `*.css` ambiently, working around a second, unrelated TS 6.0 quirk: `tsc`'s `moduleResolution: "bundler"` demands an actually-resolvable file for relative-path side-effect imports (`import "./global.css"` in `App.tsx`) even with a wildcard ambient declaration present, a stricter behavior than pre-6.0 TypeScript had — worked around with a `// @ts-ignore` on that one import line instead of fighting the compiler further, since Metro (via NativeWind's plugin) handles the actual file at build time regardless of what `tsc` thinks.
- **NativeWind v4** (Tailwind classes via `className` on RN components) — keeps the "Tailwind utility classes only" convention consistent across web and mobile. Requires `babel.config.js` (`nativewind/babel` preset), `metro.config.js` (`withNativeWind`), `tailwind.config.js`, and `global.css`, all now in `mobile/`.
- **Navigation:** React Navigation v7. `mobile/src/navigation/RootNavigator.tsx` mirrors `DashboardLayout.tsx`'s `NAV_BY_ROLE` switch on web — shows `AuthNavigator` (Login/Register, native-stack) when logged out, then swaps to one role-specific bottom-tab navigator (`VictimTabs`/`DonorTabs`/`VolunteerTabs`/`AdminTabs`) once `AuthContext`'s `profile.role` is known. Every tab navigator gets a shared `LogoutButton.tsx` in its header (`headerRight`) since there's no persistent sidebar the way `DashboardLayout.tsx` has on web.
- **`mobile/src/lib/api.ts`, `auth.ts`, `districts.ts`, `context/AuthContext.tsx`** are near-verbatim ports of their `web/src` equivalents — all pure logic with no DOM dependency, so the only real changes were import paths and `import.meta.env` → `process.env.EXPO_PUBLIC_*`.
- **Victim flow** (`mobile/src/screens/victim/`): `SubmitRequestScreen.tsx` (multi-category picker with live caps from `GET /api/categories`, `expo-location` for the geolocation capture instead of `navigator.geolocation`, the same "no active emergency in your area" soft note as web), `MyRequestsScreen.tsx` (per-item delivery status, pull-to-refresh via `RefreshControl`). **QR confirmation uses `expo-camera`'s native `CameraView` + `onBarcodeScanned`, not `html5-qrcode`** (that's a browser-only library) — a real architecture difference from web's `QrScanModal.tsx`, not just a styling port, but the actual scanned payload contract (`{deliveryId, token}` JSON → `POST /:id/confirm`) is identical.
- **`expo-camera` and `expo-location` both need config in `app.json`'s `"plugins"` array** (permission strings shown in the OS prompt) — without this, permission requests fail silently or the native module doesn't build correctly. Already added for both.
- **Local dev networking — the part most likely to trip up a future session:** a phone running Expo Go can't reach `localhost:5000` (that would mean the phone itself, not this computer). `mobile/.env`'s `EXPO_PUBLIC_API_URL` must be the computer's **LAN IP** (`ipconfig` → IPv4 Address under the Wi-Fi adapter), and the phone + computer must be on the same Wi-Fi network. **CORS is a non-issue for the mobile app** — `fetch()` from React Native doesn't send an `Origin` header the way a browser does, so Express's `cors()` middleware (which only checks `CLIENT_ORIGIN`) never blocks it; no server-side change was needed. **The one thing that *can* silently block this and can't be fixed from code: Windows Firewall.** The first time Node.js accepts an inbound connection from another device, Windows normally shows an Allow/Block prompt (or it may already be silently blocked from a past "Block" click) — if the phone can't reach the API, this is the first thing to check, not the code.
- **Verified real (not assumed):** `npx tsc --noEmit` clean; the actual Metro bundle requested directly via `curl http://localhost:8081/index.bundle?platform=android&dev=true` returns `200` end-to-end (confirms every screen, nav file, and native module actually resolves and transforms — not just that individual files type-check in isolation); the Express backend confirmed reachable at the LAN IP via `curl`. **Not yet verified: an actual phone in Expo Go.** That needs the user to run `npx expo start` themselves (from their own terminal, so they get the real interactive QR code — a background-run instance doesn't produce anything scannable) and walk the victim flow for real. Do that before trusting this is fully proven, and before starting the next role's screens.

Continuing in the same "one at a time" order as every other feature in this file: donor flow next, then volunteer, then admin, then maps/SOS/i18n/push notifications, each verified before moving to the next.

## Not started

### Photo evidence — deferred, needs a decision first
The user asked for victims to attach a photo when submitting a request, and volunteers/self-delivering donors to attach a photo at delivery confirmation (directly answers the "no post-contribution visibility" gap in the dissertation's literature review, Section 2.4.4 — worth pointing to in the viva). **Explicitly deferred by the user**, not forgotten: it needs Firebase Storage, which — unlike Firestore/Auth — requires the Firebase project to be on the **Blaze (pay-as-you-go) plan** even to stay within the free quota. The user is setting up Google Cloud billing for Maps navigation separately; flagged that enabling Storage could piggyback on that same session. Don't start building the upload flow until the user confirms Storage is enabled — there's nothing to test it against otherwise.

### Turn-by-turn volunteer navigation ("like PickMe")
Blocked on the user creating a billing-enabled Google Cloud project + API key (Directions API, Maps JavaScript API/SDK, optionally Places API for address autocomplete). Also depends on the live-location-tracking rework noted above. Don't start scaffolding this until the API key exists — a placeholder key isn't useful here the way it might be elsewhere.

### Road closures
Smaller follow-on to the DMC integration, not started. DMC's site showed a "Closed Road" bulletin link during research but it wasn't investigated — check whether it's structured data or another PDF-only bulletin (like Situation Reports turned out to be) before committing to build it.

### Extending i18n coverage
AdminRequests, AdminDonations, VolunteerDeliveries, DonorDonationForm, DonorMyDonations are still English-only — see "Internationalization" above for the pattern to follow.

## API reference

Base URL: `http://localhost:5000`. All protected routes expect `Authorization: Bearer <firebase-id-token>`.

| Method | Path | Role | Purpose |
|---|---|---|---|
| GET | `/` | none | health check |
| GET | `/api/stats` | none | aggregate counts for transparency dashboard |
| GET | `/api/stats/by-area` | none | district-aggregated severity for the public map (count + avg severity + color level, no coordinates below district-centroid granularity) |
| POST | `/api/users/profile` | any authed user | create/update profile; requires+validates nic/homeAddress for role victim |
| GET | `/api/users/me` | any authed user | get own profile |
| GET | `/api/users/volunteers` | admin | list all volunteers |
| PATCH | `/api/users/availability` | volunteer | set own availability; body `{available}` |
| POST | `/api/users/fcm-token` | any authed user | register this device's push-notification token; body `{token}` |
| GET | `/api/water-alerts/settings` | admin | current auto-send toggle for water-level area alerts |
| PATCH | `/api/water-alerts/settings` | admin | flip the auto-send toggle; body `{autoSend}` |
| GET | `/api/water-alerts/pending` | admin | unresolved water-level escalations awaiting admin review |
| POST | `/api/water-alerts/pending/:id/approve` | admin | send a pending area alert now to every matching victim |
| POST | `/api/water-alerts/pending/:id/reject` | admin | dismiss a pending area alert without notifying anyone |
| GET | `/api/water-alerts/sent` | admin | history of area alerts actually sent (auto or admin-approved), last 50 |
| POST | `/api/sos` | any authed user | submit a life-safety SOS report; body `{type, location, peopleCount?, description?}` |
| GET | `/api/sos` | admin | all SOS reports, unresolved first |
| GET | `/api/sos/mine` | any authed user | own submitted SOS reports |
| PATCH | `/api/sos/:id/status` | admin | advance status; body `{status: "acknowledged"\|"in_progress"\|"resolved"}` |
| POST | `/api/community-reports` | volunteer | submit a road-closure/water-condition report; body `{type, description, location}` |
| GET | `/api/community-reports` | admin | all reports, unverified first |
| GET | `/api/community-reports/verified` | none | verified reports only — what the public/map surfaces actually show |
| PATCH | `/api/community-reports/:id/verify` | admin | approve or dismiss; body `{approve, activateDistrict?}` — approving with `activateDistrict: true` also declares the report's district an active emergency |
| POST | `/api/requests` | victim | submit multi-item request; validates category caps, sets possibleDuplicate flag |
| GET | `/api/requests` | admin | list all, sorted by priorityScore desc |
| GET | `/api/requests/mine` | victim | own requests |
| PATCH | `/api/requests/:id/verify` | admin | approve/reject |
| PATCH | `/api/requests/bulk-verify` | admin | approve/reject several at once; body `{ids, approve}` |
| PATCH | `/api/requests/:id/status` | admin | manual status override, rarely needed |
| GET | `/api/requests/:id/notes` | admin | internal case-note thread for a request, chronological |
| POST | `/api/requests/:id/notes` | admin | add a case note; body `{text}` |
| POST | `/api/donations` | donor | register donation with deliveryMethod |
| GET | `/api/donations` | admin | list all |
| GET | `/api/donations/mine` | donor | own donations |
| POST | `/api/donations/:id/match` | admin | matches to best request still needing this category; self-delivery auto-creates+auto-accepts its delivery here |
| POST | `/api/deliveries` | admin | assign a volunteer (volunteer-delivery donations only) |
| GET | `/api/deliveries/mine` | volunteer | own deliveries, including ones awaiting accept/reject |
| PATCH | `/api/deliveries/:id/accept` | volunteer | pending_acceptance → accepted |
| PATCH | `/api/deliveries/:id/reject` | volunteer | pending_acceptance → rejected; body `{reason?}`; reopens donation for reassignment |
| PATCH | `/api/deliveries/:id/status` | volunteer | picked_up / delivered |
| PATCH | `/api/deliveries/:id/self-deliver` | donor | accepted → delivered, self-delivery only |
| GET | `/api/deliveries/by-request/:requestId` | victim | all deliveries for own request (confirmToken stripped) |
| GET | `/api/deliveries/by-donation/:donationId` | donor | delivery for own donation (includes confirmToken, for the QR) |
| POST | `/api/deliveries/:id/confirm` | victim | ownership-checked AND body `{token}` must match delivery.confirmToken (scanned from QR); updates item + recomputes request status |
| GET | `/api/external/alerts` | none | DMC Emergency Warnings, parsed from their real RSS feed (often empty — no active warning) |
| GET | `/api/external/water-levels` | none | live river gauge readings (unofficial Dept. of Irrigation proxy) with computed status vs flood thresholds |
| GET | `/api/external/gdacs` | none | GDACS global disaster events (RSS-sourced), filtered to Sri Lanka relevance by default; `?scope=global` returns every current event worldwide (for demo purposes when nothing is Sri Lanka-relevant) |
| GET | `/api/external/district-boundaries` | none | real Sri Lanka district (admin-2) boundary polygons, sourced from HDX's official COD-AB dataset, simplified server-side; cached 24h and warmed at server startup |
| GET | `/api/external/earthquakes` | none | recent magnitude 4.0+ earthquakes from USGS across the Indian Ocean/Bay of Bengal/Sumatra subduction zone — a regional tsunami-risk indicator, not a Sri Lanka earthquake feed |
| GET | `/api/external/weather` | none | current conditions for 8 cities spanning Sri Lanka's main regions, from OpenWeatherMap — returns `[]` without `OPENWEATHER_API_KEY` set |
| GET | `/api/categories` | any authed user | current category limits, `{[key]: {label, max, unit}}` — self-seeds Firestore on first call |
| POST | `/api/categories` | admin | add a new category; body `{label, unit, max: number\|null}` |
| PATCH | `/api/categories/:key` | admin | adjust an existing category's cap/label/unit; body `{max?, label?, unit?}` |
| GET | `/api/broadcasts/active` | none | current emergency banner, or `null` if none is active |
| GET | `/api/broadcasts` | admin | broadcast history, most recent 20 |
| POST | `/api/broadcasts` | admin | post a new banner (deactivates any current one); body `{message, severity?}` |
| PATCH | `/api/broadcasts/:id/deactivate` | admin | clear the active banner without posting a replacement |
| GET | `/api/audit-log` | admin | last 100 logged actions (approve/reject/bulk/match/assign/district), most recent first |
| GET | `/api/active-districts` | none | list currently active-emergency districts |
| POST | `/api/active-districts` | admin | activate a district; body `{district, sourceAlertTitle?}` |
| DELETE | `/api/active-districts/:district` | admin | deactivate a district |

## Running it

```bash
cd server && npm install && npm run dev     # http://localhost:5000
cd web && npm install && npm run dev        # http://localhost:5173
```
Requires `server/.env` with `FIREBASE_SERVICE_ACCOUNT_PATH` (forward slashes even on Windows) and `web/.env` with `VITE_FIREBASE_*` values. Once Google Maps navigation work starts, `web/.env` will also need a Maps API key — not yet added, ask the user for it rather than assuming a placeholder works.

## Manual end-to-end test procedure

No automated tests exist yet. Confirmed working live in the browser as of this handoff:
1. Register a victim — NIC + home address required; a 2nd victim at the same address submitting a request while the 1st has an active one gets `possibleDuplicate: true` (flagged, not blocked)
2. Victim submits a multi-category request (e.g. food + water) — per-category caps enforced server-side even if the client is bypassed; a medicine item shows no cap but flags "⚕ Contains medicine — review" for admin
3. Admin approves at `/admin/requests`
4. Donor registers a donation picking "Find a volunteer" or "I'll deliver it myself"
5a. **Volunteer path:** admin matches then assigns a volunteer; volunteer must Accept or Reject (optional reason) at `/deliveries/mine` — a reject reopens the donation for reassignment and shows the admin the reason; then picked_up → delivered
5b. **Self-delivery path:** admin matches (delivery auto-created+accepted); donor marks "Mark as delivered" at `/donations/mine`
6. Victim scans the QR code (shown by the volunteer or self-delivering donor once status is "delivered") via "Scan to confirm" at `/request/mine` — a wrong/missing token gets rejected (403), verified directly against the API; request only flips to "Delivered" once every item is confirmed
7. `/transparency` and `/severity-map` both reflect the updated data; `/admin/map` shows exact pinpoints
8. Language switcher (EN/SI/TA) works on all translated pages and persists across reloads

## Status as of this handoff

**Working end-to-end:** the full loop above, including multi-item requests, both delivery methods, volunteer accept/reject with a real reassignment cycle, all three map surfaces (now with real district boundary polygons on the public severity map, not just centroid circles), live DMC alerts + river gauge data + GDACS global disaster alerts + USGS tsunami-risk earthquakes + OpenWeatherMap current conditions, QR-code delivery confirmation (backend token validation verified directly, camera-based scanning verified up through QR generation — actual camera-scan-to-confirm round trip not exercisable in this dev environment, only the API contract), English/Sinhala/Tamil language switching on the translated pages, admin-editable category caps/new categories, the emergency broadcast banner, internal per-request case notes, live Firestore updates on AdminRequests/AdminDonations/the banner, bulk approve/reject, filters/search on the requests table, the resource gap view, stale request flags, the audit log, CSV/PDF export on both admin tables, the volunteer workload view, the volunteer availability toggle, the admin dashboard KPI row, per-district active-emergency declarations feeding both the victim-facing soft note and the public banner, Firebase Cloud Messaging push notifications on the delivery lifecycle (request approved / picked up / delivered), admin-controlled water-level area alerts with a live auto-vs-review toggle, a separate SOS/rescue flow with a live dispatch board, and volunteer-submitted community reports that can promote straight into an active-district declaration (see "Category management", "Emergency broadcast banner", "Internal case notes", "Live updates", "Bulk approve/reject", "Filters and search", "Resource gap view", "Stale request flags", "Audit log", "CSV/PDF export", "Volunteer workload view", "Volunteer availability toggle", "Admin dashboard KPIs", "Active emergency districts", "GDACS disaster alerts", "Real district boundary polygons", "USGS earthquakes", "Weather (OpenWeatherMap)", "Push notifications (FCM)", "Water-level area alerts", "SOS / rescue flow", and "Community reports" above).

**Every item on the user's original "do it one at a time please" list is done**, plus follow-on features built after: ~~admin-editable category limits~~ → ~~emergency broadcast banner~~ → ~~internal case notes per request~~ → ~~live Firestore `onSnapshot` updates on admin pages~~ → ~~bulk approve/reject~~ → ~~filters/search~~ → ~~resource gap view~~ → ~~stale request flags~~ → ~~audit log~~ → ~~CSV/PDF export~~ → ~~volunteer workload view~~ → ~~volunteer availability toggle~~ → ~~admin dashboard KPIs~~ → ~~active emergency districts~~ → ~~GDACS disaster alerts~~ → ~~GDACS cyclone track polygon on the admin map~~ → ~~real HDX district boundary polygons on the public severity map~~ → ~~USGS earthquake tsunami-risk indicator (own map tab on /admin/map, not just a list panel)~~ → ~~Firebase Cloud Messaging push notifications on request/delivery status~~ → ~~admin-toggleable water-level area alerts~~ → ~~separate SOS/rescue flow~~ → ~~volunteer community reports with district-activation promotion~~ → ~~OpenWeatherMap weather integration~~ (all done). **Real SMS specifically was requested again in this pass and re-confirmed as out of scope** — the user chose FCM over taking on a paid Twilio account when asked directly; don't silently reopen that without asking again. No further items are queued — check with the user before starting new feature work rather than assuming what's next.
**Not started:** real SMS (Twilio, needs a paid/verified business account — deliberately not built, see above and "Known scope cuts" below), photo evidence (deferred, needs Storage/Blaze decision), Google Maps turn-by-turn navigation (blocked on billing setup), deployment, evaluation/SUS prep, DMC situation-report/water-level PDF parsing (deliberately skipped), i18n coverage on admin/donor/volunteer internal pages (web) and on the mobile app entirely. **In progress:** the mobile app (Expo/React Native) — navigation, auth, and the victim flow are built and compile cleanly; donor/volunteer/admin screens are still placeholder stubs. See "Mobile app (Expo / React Native)" above. **Three things need a value/action from the user before they're fully live**: (1) `VITE_FIREBASE_VAPID_KEY` in `web/.env` (Firebase Console > Project Settings > Cloud Messaging > Web configuration) — without it push notifications silently no-op, see "Push notifications (FCM)" above; (2) the updated Firestore rules (new `sosRequests` block) need `firebase deploy --only firestore:rules` — asked the user for confirmation, hadn't heard back as of this handoff, see "SOS / rescue flow" above for exactly what's blocked until then; (3) `OPENWEATHER_API_KEY` in `server/.env` (free tier, openweathermap.org/api) — without it the weather feed returns `[]`, see "Weather (OpenWeatherMap)" above.

## Known scope cuts already agreed with the user (don't relitigate these)

- SMS → Firebase Cloud Messaging push notifications
- No full route optimization for matching — Haversine straight-line distance as a tiebreaker, priority score is the primary sort key
- No AI/ML — explicitly out of scope per the dissertation itself

## Known scope DEVIATION (the opposite of a cut — flagged, not hidden)

The dissertation's Chapter 1 "Out of Scope" explicitly excludes "Government Database Integration." The user chose to build DMC/Irrigation Dept data integration anyway (this is a research project, not deployed publicly). **Action still pending from the user: update the dissertation's Out of Scope wording so it doesn't contradict the actual system.** This was explicitly flagged to them as the real risk, not a technical one — worth checking if it's been done if it comes up again.

## Privacy and security notes — non-negotiable constraints

- **NIC number and home address are sensitive PII.** Never expose either in: the public transparency dashboard, the public severity map, any API response reachable without `requireRole("admin")` or the record's own owner, or any client-side console log left in committed code. In any admin list view, consider masking (e.g. show only the last 4 digits of the NIC) with full value visible only on a dedicated detail view. Firestore rules must restrict the `users` collection fields the same way the existing rules already restrict profile documents to their own owner or admin.
- **Victim GPS coordinates are similarly sensitive** — this is why `SituationMap.tsx` (admin, exact pins) and `PublicSeverityMap.tsx` (public, district aggregates only) are two separate pages backed by two separate endpoints, not one page with a role check. Don't let a "quick win" shortcut (e.g. reusing the admin map's data source for the public page) leak exact locations publicly.
- **Correction to a long-standing wrong assumption:** earlier handoffs of this file claimed Firestore was "still in test mode" with `firestore-rules/firestore.rules` unedeployed. That was wrong — while building live `onSnapshot` listeners (see "Live updates" below), a direct unauthenticated Firestore REST read against `aidRequests` succeeded and a read against `broadcasts` (a collection with no rule block at the time) failed with `permission-denied`, which is only possible if real rules matching this file were already deployed and actively enforced. Nobody had verified this either way before; it was just repeated across handoffs. `firestore-rules/firestore.rules` is now confirmed deployed via `firebase deploy --only firestore:rules` (project `disaster-aid-system`, config in `firebase.json`/`.firebaserc` at the repo root) and covers all six collections: `users`, `aidRequests`, `donations`, `deliveries`, `broadcasts`, `categoryLimits`, `caseNotes`. **Lesson for future sessions: don't take a prior handoff's infrastructure claims (deployed/not deployed, configured/not configured) at face value — verify empirically when it becomes load-bearing, the way this was.**

## Coding conventions established so far

- Every backend route validates required fields and returns `{ error: "..." }` with an appropriate HTTP status on failure.
- Frontend pages: self-contained function components, `useState`/`useEffect` only, no external state library.
- `apiFetch()` in `web/src/lib/api.ts` is the only sanctioned way the frontend calls the backend.
- Tailwind utility classes only.
- Firestore field names camelCase; every write sets `createdAt`/`updatedAt` as ISO strings, not Firestore Timestamps.
- New rule going forward: any field or endpoint touching NIC, home address, or exact victim location gets a one-line comment noting it's sensitive and why it's restricted the way it is — makes the constraint visible to whoever edits that code next, not just documented here.

## Open decisions the user still needs to make (don't guess silently on these)

1. Google Maps budget comfort level, for setting a sensible Cloud budget alert threshold — needed before navigation work can start at all.
2. The Firebase Cloud Messaging Web Push certificate (VAPID key) — Firebase Console > Project Settings > Cloud Messaging tab > Web configuration > generate a key pair, then paste it into `VITE_FIREBASE_VAPID_KEY` in `web/.env`. Push notifications are fully built and wired (see "Push notifications (FCM)") but silently no-op without this — nothing to guess or placeholder around, it's tied to the user's own Firebase project the same way the Maps API key is.
3. An OpenWeatherMap API key — free tier, no credit card needed, sign up at openweathermap.org/api, then paste it into `OPENWEATHER_API_KEY` in `server/.env`. The weather feature is fully built (see "Weather (OpenWeatherMap)") but returns `[]` without this.

Resolved during the maps build, for reference: fairness caps are set (see `categories.js`); self-delivering donors do still go through the priority-matching algorithm, they just skip the volunteer-assignment step; the public map aggregates by district via nearest-centroid matching, not a grid (see "Maps — built" above for why).
